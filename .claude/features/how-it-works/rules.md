# How It Works: Rules & Constraints

- Preserve the 3-step Done-For-You card layout (`HiwProcessGrid.tsx`).
- Step 1 Rex avatar and Step 4 Crew avatars must use clean `/images/mascots/*.png` images with `object-contain`.
- Emphasize zero technical work required from the client's office team.
