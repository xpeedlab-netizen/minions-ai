# Pip: Rules & Constraints

- Mascot image is FIXED: `/images/mascots/pip.png` with `object-contain`.
- Strict No-Guess Policy: Grounded 100% in company knowledge base; never hallucinates prices or policies.
- PipChatWidget inline panel MUST accept `hideHeader` prop to prevent header duplication when rendered inside hero animations.
- Output text in No-Guess cards uses luminous green (`text-[#4ade80]`) and matching background tint (`bg-[#4ade80]/15`) for high contrast.
