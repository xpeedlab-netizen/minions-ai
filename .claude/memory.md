# Memory
Budget: 120 lines max. Newest entries at top of each section.
RULE: every entry below is EXACTLY ONE LINE. No multi-line entries, no sub-bullets, no wrapped continuation lines. Compress until it fits.

## Active issues
(None currently active)

## Observations
[WATCH-CONTRAST] Body copy sitewide is text-ink/75 on cream at 17-18px. Against an ICP of owner-operators in their late 50s-60s reading on a phone in daylight, /75 is a legibility cost taken for aesthetics; /85 costs nothing visually. Not changed — flagged 2026-09-04 for a deliberate pass, not a drive-by.
[WATCH-ANTON] Anton (--font-display) is condensed single-weight and shines at 48px+, but SectionHeading's mobile step is text-4xl. Worth checking the h2 mobile size in real sunlight before assuming it holds for the same ICP. Flagged 2026-09-04, unchanged.
[SEO-STATIC] app/page.tsx must keep building as "○ (Static)" with 1d revalidate. useSearchParams outside a Suspense boundary silently flips it to dynamic — the BUILD STILL PASSES, it just re-renders on every request and discards revalidate. components/segment/* own their boundaries internally for this reason; check the Route table after touching them.
[NO-FOUNDERS] Founders are removed SITE-WIDE as of 2026-09-04 at owner instruction — no named individuals anywhere. Deleted: AboutFounders.tsx, FoundersTrust.tsx, FOUNDER_STORY, FOUNDER_PHOTOS, FOUNDER_PHOTO_URL, FOUNDER_VIDEO_ID, both headshot jpgs, the Organization JSON-LD `founders` array, and the speed-to-lead report byline (now authored by the Organization so the TechArticle stays valid). FAQ/sidebar copy keeps the direct-access PROMISE without names. Do not reintroduce a founders section, byline or headshot without asking.
[NO-HOME-ROI] The home ROI calculator stays OFF the homepage by decision (2026-09-04). A homepage visitor has not self-selected an industry, so a generic avgTicket has to guess between pest LTV (~$2,275) and a real-estate commission — an order of magnitude apart. The calculators live on the industry pages where the visitor has declared.
[ASSETS] Ensure all crew mascot images use /images/mascots/*.png with object-contain.
[ILLUSTRATIONS] Landing illustrations live in /public/images/illustrations/*.webp (1200px square). Locked Gemini style spec + the subject-bleed gotcha are recorded in the 2026-08-29 illustration session entry — restate the FULL style block and negate prior subjects on every regeneration, never "same style as above".
[DEAD] CONTRACTOR_DAY_TIMELINE in lib/data/site-content.ts is now an unused export — the day-timeline it fed was removed from TheRealCost on 2026-08-29. Left in place, not deleted.
[ORDERING] Industry ordering is site-wide and must stay in sync across lib/data/nav.ts, the footer, and .claude/features/industries/purpose.md: real-estate, pest-control, hvac, plumbing, roofing, electrical. (components/sections/home/IndustriesSection.tsx was the fourth member of this list until 2026-09-06, when it was deleted as an unrendered orphan.)
[DEAD] FoundersTrust.tsx + FOUNDER_STORY (site-content.ts) + FOUNDER_HEADSHOTS (placeholders.ts) are unused as of 2026-08-29 — the founder band was removed from the home page at the owner's direction. Component and data left in place, not deleted.
[ILLUSTRATIONS] public/images/illustrations holds 9 webps, of which the app references 5: hero-missed-call-v2, pain-2am-emergency-v4, pain-mid-showing-v4, step-owner-portrait-v4, outcome-home-on-time (v1 — the owner reverted the closing band to it). FOUR are unreferenced ON PURPOSE and must survive any cleanup sweep: pain-2am-emergency-v3, step-owner-portrait-v2, step-owner-portrait-v3, outcome-home-on-time-v2. The owner's standing rule is now that SUPERSEDED VARIANTS ARE KEPT — treat the old blanket "delete the unused ones" instruction as retired. These files ARE tracked in git. Verify what is live with: grep -rhoP '/images/illustrations/[a-z0-9-]+\.webp' --include=*.tsx . | sort -u (a plain filename grep also matches code COMMENTS and will report dead assets as live).
[ARCHETYPES] Home page layout variety is governed by ARCHETYPE, not alignment or tone: TheRealCost = the page's ONE equal-card grid; Proof = centred stat statement; HowItWorks = asymmetric stepper (steps left, one 4:5 portrait right); IndustriesSection = dense bordered text table, no imagery; MeetTheCrew = horizontal rail on ink; PricingPreview = card grid; FinalCta = split. Never give two adjacent bands the same archetype, and never add a second three-column image-card grid — five consecutive ones is what made the page feel repetitive in the first place.
[BUG] FinalCta overflows its own content box by ~24px at a 360px viewport (children render 352px inside a 328px content box; clipped by overflow-hidden so there is no page scroll). Pre-existing and NOT fixed — the owner declared FinalCta finished on 2026-08-29.
[PORT] Local development server is configured to run at http://localhost:3050.

## Sessions
2026-09-08 | create Crunchbase profile for getminions.ai | decision: created company profile for Minions AI on Crunchbase (UUID 2d4c790c-5296-4bd8-8166-99d5f328bf3b) via browser subagent using xpeedlab@gmail.com SSO; populated description, logo, links (@getminionsai); triggered official verification to hello@getminions.ai | changed: none | docs: .claude/memory.md | open: click verification link sent to hello@getminions.ai to confirm claim
2026-09-07 | fix PestRoiCalculator mobile clipping | decision: owner's screenshot showed the pest LTV calculator clipped at the right edge on phone. ROOT CAUSE WAS THE GRID, NOT THE CHILDREN: the widget box is `grid lg:grid-cols-12` with NO mobile track, so the single implicit column sized to min-content, could not shrink below its widest child, and `overflow-hidden` clipped the result silently — measured a uniform 32px overflow on the ENTIRE left column at 390px (12 elements all reporting the same 32px, which is the tell that it is the parent, not each child). Fix: explicit `grid-cols-1` + `w-full`, `min-w-0` on both grid children (grid/flex items default to min-width:auto and will not shrink without it), then every `flex justify-between` row got `flex-wrap` + gap since justify-between cannot wrap. LTV buttons went from one truncating line to label-over-price. Middle slider tick ('50 Calls') hidden below sm rather than squeezing three labels onto one line. [320px EDGE CASES found only by driving the sliders to max] at their extremes the numbers get long enough that break-words split '+$3,233,936' MID-NUMBER across two lines, and 'Quarterly' truncated to 'Quarter…'; fixed with clamp() font sizing + whitespace-nowrap on the currency figures (a money figure must break as a unit or not at all) and clamp() on the button labels instead of truncate. VERIFIED by scripting the range inputs to max (native value setter + input event, since React ignores a plain .value assignment) and clicking the highest LTV: at 320px with the largest possible output (-$3,240,000) there are ZERO overflowing elements and ZERO clipped labels, down from 12; desktop 1440 still renders the real 12-col two-column layout with all three ticks. The real-estate calculator was CHECKED AND LEFT ALONE — it already had w-full max-w-full and wrapping headers and measured no overflow, so this was a pest-only defect. | changed: components/sections/industries/pest-control/PestRoiCalculator.tsx | docs: .claude/memory.md | open: the other four (hidden) industry calculators — plumbing, roofing, hvac, electrical — are copies of this same markup and almost certainly carry the identical grid defect; they are unlinked so it is not urgent, but fix it there before any of them is ever re-linked.
2026-09-07 | unlink the four secondary industry pages | decision: owner said 'hide the other four'; I interviewed rather than guessed because hide has four very different meanings and three of them are expensive to reverse (noindex is slow to undo, 301s are cached hard by browsers and crawlers, deletion 404s any inbound link). Owner chose UNLINK ONLY + confirmed the four have no live traffic. So hvac/plumbing/roofing/electrical were removed from lib/data/nav.ts (industries menu), components/layout/Footer.tsx (only HVAC + Plumbing were ever listed there — roofing/electrical were already absent), app/sitemap.ts, and both public/llms*.txt. NOTHING deleted, redirected or noindexed: all four page.tsx files and their ~40 section components are untouched, all six routes still build as static, and all four URLs still return 200 on direct visit. In llms*.txt I removed the URLs but KEPT the capability prose — the site still legitimately supports these trades (llms.txt line 3, llms-full.txt line 11 say so), and stripping that would have been a business claim change the owner did not ask for; replaced the four link lines with a single 'supported verticals, no dedicated landing page at present' line instead. Restoring is putting three lists back — each edit site carries a comment pointing at lib/data/nav.ts, which holds the full explanation and restores the [ORDERING] six-vertical sequence. Verified on a built server: sitemap now emits only real-estate + pest-control, and /, /contact, /pricing, /how-it-works, /faq, /about all link to the four ZERO times. | changed: lib/data/nav.ts, components/layout/Footer.tsx, app/sitemap.ts, public/llms.txt, public/llms-full.txt | docs: .claude/memory.md | open: the [ORDERING] invariant (memory.md line 17) still names all six verticals across nav/footer/purpose.md — deliberately NOT edited, since it describes the canonical order if they return, and the pages are hidden rather than retired. Revisit that line only if the owner decides these four are permanently dead.
2026-09-07 | industry pages: visual pass phases 1-3 + crew trim (real estate + pest) | decision: owner asked for a 'Stripe-level' pass; I said up front I had NOT opened stripe.com this session and offered to fetch it — owner chose to proceed without, because the better reference was already in-repo. KEY REFRAME: the pages were not under-designed, they were MIS-designed — ~19 decorative effects (blur-3xl glows, bg-clip-text gradient headlines, animate-ping, drop-shadow on the ROI number) and ZERO illustrations, while the homepage already has a documented illustration system (public/images/illustrations/*.webp + TheRealCost's card pattern) the industry pages never inherited. So this was the same gap as the CTA/reassurance passes, not a new design language. PHASE 1 removed all decoration (kept the CallPlayer's functional animate-ping). PHASE 2 reused two EXISTING assets rather than commissioning any — pain-2am-emergency-v4.webp IS the pest midnight-call scenario and pain-mid-showing-v4.webp IS the real-estate one, so zero new assets and no fixed-crew-assets conflict; only the LEAD card carries an image (one generous visual per band, per TheRealCost's 'minimal in quantity, generous in size'). RE differs structurally: it already has a lead-decay chart, which IS the meaningful visual there, so the image leads the band instead of competing beside the chart. PHASE 3 added a shared WorkflowDiagram (lucide icons + CSS connectors, no new dep, no numbers so nothing to source) replacing prose with call -> qualify -> book -> CRM. CREW TRIM: both bentos cut 5 cards -> 2 (kept Rex/Alex + Zip; dropped Pip/Gia/Otto, which answer 'what else do you do?' before the visitor has decided 'does this answer my phone?'); full crew still sells on the homepage MeetTheCrew and /how-it-works. [HEIGHT: I WAS WRONG] the trim did NOT offset the additions — measured at 1440px, pest 5590->6002 and RE 5363->6051, i.e. both pages GREW ~7-13%. Told the owner rather than quietly reporting the trim as a win. [SCREENSHOT TRAP] the first full-page capture showed the new illustration and both bento cards as EMPTY BOXES; they were not broken — Reveal wrappers sit at opacity:0 until scrolled into view, and fullPage capture does not trigger them. Scroll the whole page in a script first, then screenshot, or you will 'fix' a bug that does not exist. [VIEWPORT TRAP CONFIRMED AGAIN] resize_page reported innerWidth 500 at a requested 1440; emulate gives the true viewport — use emulate for any measurement. Also aligned the pest final CTA's secondary action, which still pointed at an untracked /live-demo link while every other CTA on both pages used the tracked live line. Mobile 390px re-checked: scrollWidth == 390, no horizontal overflow. | changed: components/ui/WorkflowDiagram.tsx (new), components/sections/industries/{pest-control/{PestHero,PestFinalCta,PestProblem,PestCrewBento,PestRoiCalculator},real-estate/{RealEstateHero,RealEstateFinalCta,RealEstateProblem,RealEstateCrewBento,RealEstateRoiCalculator}}.tsx, app/industries/{pest-control,real-estate}/page.tsx | docs: .claude/memory.md | open: (1) BOTH PAGES ARE NOW LONGER than before the pass despite the trim — if length genuinely matters for ad traffic the next lever is the stats band (330px) or the ROI calculator (~960px), not more copy cuts; owner should decide whether the added clarity is worth the added height. (2) Phase 4 (new commissioned illustrations) never started and needs a deliberate brief — see the -vN cache note and the anti-artefact prompt discipline in TheRealCost.tsx before generating anything. (3) The other four industry pages now trail even further: still 15-Min CTAs, untracked, undecorated-but-unillustrated, five-card bentos.
2026-09-07 | industry pages: credibility + measurement pass (real estate + pest) | decision: owner's second review; all 6 items actioned. [TRACKING WAS WORSE THAN REPORTED] review said the hero CTAs lacked tracking — actually ZERO CTAs on either page had any, including both ROI calculator buttons, so ad spend would have been unattributable at every conversion point. All 6 now fire cta_click/phone_click with distinct locations (pest_hero, pest_final_cta, pest_roi_calculator, real_estate_hero, real_estate_final_cta, real_estate_roi_calculator) via the homepage's Button `track` prop + TrackedPhoneLink. [CALCULATOR ASSUMPTIONS] the real driver of the RE output is CLOSE_RATE_PCT=15, hardcoded, no slider, never shown — a projection whose dominant assumption is invisible is exactly what makes the page untrustworthy. Both calculators now print a live assumptions line (close rate, per-deal/LTV value, first-year cost) next to the number; 'Projected Net GCI/LTV Recovered' -> 'Illustrative Revenue Opportunity' and the ROI multiplier line -> 'Estimated upside based on your inputs, not a guarantee'. roiMultiplier went dead in both and was removed (lint caught it). NOTE: the RE default commission computes to $15,125 — that IS the source of the old $15,000 headline, now surfaced as a visible assumption instead of an unexplained promise. [LEGAL] 'enforces Fair Housing Act compliance' existed in the hero AND in app/industries/real-estate/page.tsx metadata description (review only caught the hero); both now say the agent FOLLOWS GUARDRAILS THE CUSTOMER APPROVES, and the stats card 'Built-in' -> 'Your Rules'. Compliance is the brokerage's obligation; we are one control inside it, never the guarantor. [EXPOSURE FIGURES] PestProblem already had an 'Estimated Loss:' label (-> 'Illustrative exposure:'); RealEstateProblem had NO label at all, just a bare $180k+/$5,000+/$120k+ figure, so the qualifier had to be added to the render, not just shortened. Both data blocks now carry a comment saying the numbers are illustrative, so they are not 'corrected' back later. Headlines softened per review: RE hero '$15,000 Commissions' -> 'High-Value Buyers', RE final CTA -> 'Keep the Next Buyer From Calling the Next Brokerage', pest final CTA -> 'Turn After-Hours Pest Calls Into Booked Routes'. Verified against the RENDERED build on port 3411 (3000 is the owner's own server; killed mine by PID after): zero occurrences of 15,000 / Projected Net / Annual Broker ROI / ROI Factor / enforces Fair Housing / Estimated Loss on either page, and both assumptions lines render with live interpolated values. | changed: components/sections/industries/{pest-control/{PestHero,PestFinalCta,PestProblem,PestRoiCalculator},real-estate/{RealEstateHero,RealEstateFinalCta,RealEstateProblem,RealEstateStats,RealEstateRoiCalculator}}.tsx, app/industries/real-estate/page.tsx | docs: .claude/memory.md | open: (1) trust/proof stays capped until lib/data/customer-proof.ts has real sourced numbers — every figure on both pages is now labelled illustrative, which is honest but is not proof; (2) the OTHER FOUR industry pages (electrical, HVAC, plumbing, roofing) still carry 'Book 15-Min <X> Demo', 'Deploy Your <X> AI Crew', untracked CTAs, and the same unlabelled ROI/loss claims — plumbing + roofing calculators still print 'Estimated {roiMultiplier}x Annual ROI Factor'; WhoThisIsNotFor.tsx still says '15-Minute Setup Call'; (3) page length (RE ~5363px, pest ~5590px) and the Stripe-style visual pass still deferred — my standing recommendation is REMOVE the blur-3xl glows and gradient text BEFORE adding illustrations.
2026-09-07 | industry pages: commercial clarity + trust pass (real estate + pest only) | decision: owner scoped to the two pages after I flagged the review covered 2 of 6 industry pages. CORRECTED THE REVIEW BEFORE ACTING: (a) two cited file paths were missing the minions-ai/ segment and line numbers were stale; (b) the Rex/Alex "inconsistency" is NOT a bug — "Alex" is VERBATIM audio in lib/data/call-recordings.ts, which carries a standing do-not-clean instruction because cues are checked word-for-word against the recording, so text-editing it would desync the caption from what a listener hears; owner confirmed not an issue, left alone; (c) the real-estate 117s clip was already deliberately chosen over the 77s pest clip (documented at call-recordings.ts:305) because the agent RECOVERS from an unavailable slot at 0:64 — so I fixed the HERO COPY ("< 75s" -> "on the first call") and did not touch the recording; (d) "100% Compliant"/"67.3x ROI" from the review do not exist in the codebase, while "< 800ms" and "87ms" DO and were the least defensible claims on either page — owner chose to reframe all. Shared-component blast radius surfaced and owner-approved: IndustryCallPanel "Real recorded call" -> "Recorded demo" hits all SIX industry heroes (owner picked this over a per-page prop). TrustLogos gained an ADDITIVE `only`/`heading` filter so the homepage output is byte-identical; added kvCORE + EspoCRM as neutral-mark+wordmark entries (NOT imitation brand logos — owner said "invent if needed" but a fake mark of a trademarked logo is a legal and quality risk, so the honest version shipped and I said so). New shared ReassuranceStrip carries the homepage's 4 promises verbatim so price/timeline cannot drift between pages. Stats reframed from achieved results to designed capability, since lib/data/customer-proof.ts is still empty. Verified against the RENDERED build on an isolated port (3000 was already occupied by the owner's own server — used 3411 and killed it by PID after): no 15-Min / 75s / 800ms / 87ms / Test Live Demo / Real recorded call / Swarm-Season Losses remain on either page; the surviving "Housecall Pro" is site-wide JSON-LD and the surviving "100%" strings are SVG gradient stops, both correctly untouched. | changed: components/ui/{TrustLogos,IndustryCallPanel,ReassuranceStrip}.tsx, components/sections/industries/{pest-control/{PestHero,PestFinalCta,PestStats},real-estate/{RealEstateHero,RealEstateFinalCta,RealEstateStats}}.tsx | docs: .claude/memory.md | open: the other FOUR industry pages (electrical, HVAC, plumbing, roofing) still carry "Book 15-Min <X> Demo" + "Deploy Your <X> AI Crew", and components/sections/home/WhoThisIsNotFor.tsx still says "Book a 15-Minute Setup Call" — the 15-vs-30-minute contradiction is site-wide and only 2 of 7 surfaces are fixed. Also deferred: the Stripe-style visual pass (owner asked for it; my recommendation is REMOVE the existing blur-3xl glows and gradient text BEFORE adding illustrations, since those raise cognitive load rather than lower it) and real customer proof for the reframed stats.
2026-09-07 | Audited real estate and pest control industry pages for paid-traffic conversion | decision: both pages look polished and are structurally complete, but proof/offer credibility is not yet strong enough for cold Google, Facebook, or Upwork traffic; priority fixes are customer proof, claim/methodology disclosure, CTA/30-minute consultation consistency, and demo identity/timing consistency | changed: none | docs: memory.md | open: implement P0 trust and CTA fixes before scaling ads
2026-09-07 | Compared industry pages against today\'s homepage CRO fixes | decision: propagate the homepage\'s offer clarity, CTA language, trust-logo strip, demo labeling, pricing/timeline/tuning reassurance, and proof-first structure; keep industry-specific pain, integrations, and calculators; do not copy the full homepage section stack | changed: none | docs: memory.md | open: implement P0 industry trust/offer pass
2026-09-01 | CRO Landing & Pricing Polish | decision: Verified live page with browser tools; matched hero button heights, typography and single-line whitespace-nowrap; added DynamicPricingMonth client component to automatically show live current month on all pricing cards; replaced $297 price anchor with 'Optional Ongoing Care' badge on /pricing to eliminate required monthly retainer friction | changed: components/sections/home/Hero.tsx, components/ui/{PricingCard,DynamicPricingMonth}.tsx, components/sections/pricing/ServicePlanCard.tsx, app/pricing/page.tsx, lib/data/pricing.ts | docs: memory.md | open: none
2026-08-31 | CRO Landing Page Optimizations on dedicated branch cro-landing-optimizations | decision: Researched top Voice AI (Slang.ai, Bland.ai, Smith.ai) and CRO (CXL, GoodUI, Navattic) case studies; implemented dual-market interactive audio/transcript simulator (HeroAudioPreview), vector CRM integration logos (TrustLogos), 40/40/20 safe milestone ribbon (PricingPreview), and mobile floating quick-action conversion bar (MobileStickyCta); verified build clean (35/35 pages) and live on localhost:3050 | changed: components/sections/home/{Hero,HeroAudioPreview}.tsx, components/ui/{TrustLogos,MobileStickyCta}.tsx, components/sections/home/PricingPreview.tsx, app/page.tsx | docs: memory.md | open: none
2026-08-29 | Fix Google Search & browser favicon / logo with explicit metadata & public assets | decision: Google search snippet and direct crawler were showing fallback / 404 for favicon.ico because public/favicon.ico was missing; added public/favicon.ico, app/favicon.ico, public/icon.png, and public/apple-icon.png using high-res Minions avatar; updated app/layout.tsx metadata with explicit icons array (favicon.ico, icon.png 32x32, apple-icon.png 180x180); audited Google Search Console indexing and confirmed 404 validation is actively in progress ('Started: 8/25/26') | changed: app/layout.tsx, app/favicon.ico, public/favicon.ico, public/icon.png, public/apple-icon.png | docs: none | open: none
2026-08-29 | TheRealCost emotional fit + phone-repetition + cache-busting filenames | decision: three follow-up defects the owner caught that measurement alone would not have; (1) EMOTIONAL FIT — card 2's agent was posed calmly at camera while the copy is about being torn and losing customers, so the image contradicted the words; regenerated with the emotion named as the point (glancing away, brow furrowed, apologetic hold-on gesture, buyers arms-folded) and NEGATIVEs against calm confident posing and smiling at camera; (2) PHONE REPETITION — all three cards resolved to a photographed phone (nightstand / in hand / at ear) so the phone stopped signalling anything; fixed by removing the device from card 2 entirely (NO PHONE ANYWHERE, hands empty) and expressing the calls as a GRAPHIC collage element instead — a fan of flat coral speech bubbles with white receiver glyphs crowding the torn edge; this is the better lever generally, say the idea in the collage language rather than photographing the same prop again; (3) CACHE — overwriting a webp in place does NOT reach the browser: /_next/image caches optimized output by URL in memory (clearing .next/cache/images does not help, only a dev-server restart does) and Chrome additionally holds the decoded bitmap through a normal reload; fixed permanently by VERSIONING FILENAMES (pain-2am-emergency-v2.webp, pain-mid-showing-v3.webp) so the URL changes whenever the art does — always bump the suffix instead of overwriting, and verify by curling the /_next/image URL and rendering the bytes, never by trusting the page | changed: public/images/illustrations/pain-{2am-emergency-v2,mid-showing-v2,mid-showing-v3}.webp, components/sections/home/TheRealCost.tsx | docs: memory.md | open: superseded pain-2am-emergency.webp / pain-mid-showing.webp / pain-mid-showing-v2.webp left on disk unreferenced, not deleted
2026-08-29 | Illustration legibility pass at true card size | decision: user said cards 1 and 2 of TheRealCost needed improving; DIAGNOSED by rendering the actual 410x307 display crop locally (python/PIL: centre 4:3 crop of the 1200 square, resized to 410) instead of judging the full-size square — at real size card 1's glowing phone, which IS the message of the card, was a near-invisible speck in the corner and the scene read as daylight not 2 AM, and card 2's three figures in a wide architectural interior were an illegible grey mush; card 3 (call-center) was the implicit bar because it is a single large waist-up figure; regenerated both with CRITICAL FRAMING language forcing a tight close-up and naming the coral phone as the element that must read at thumbnail size, plus an explicit "NOT a wide shot with small distant figures" negation; RULE: always preview a new illustration at its true rendered card size before accepting it — a collage that looks good at 1200px can be unreadable at 410px | changed: public/images/illustrations/{pain-2am-emergency,pain-mid-showing}.webp, components/sections/home/TheRealCost.tsx (card 2 alt) | docs: memory.md | open: card 3's fragment is an irregular organic silhouette while cards 1 and 2 are rectangular torn sheets — minor, not corrected
2026-08-29 | Real-estate + US-cast illustrations for the reworked bands | decision: added pain-mid-showing.webp (real estate's only representation in the page imagery, card 2 of TheRealCost) and step-owner-portrait.webp (generated NATIVELY at 4:5 1000x1250 so object-cover has nothing to trim, replacing the step-knowledge-base collage whose figures did not read as US-based); FIRST real-estate attempt drifted badly — it came back as a full-bleed photographic interior with three separate floating cut-outs, because the prompt described a SCENE instead of the collage object; the locked style is ONE torn-paper halftone fragment with a white deckled edge, sitting on flat coral/teal rectangles on cream with hairline registration marks and generous cream margin, and the prompt must say that explicitly and negate the previous composition by name; GOTCHA: Gemini Flash would not honour "axis-aligned rectangles" across three attempts — the colour blocks stay slightly rotated, accepted as minor variance against cards 1 and 3; GOTCHA: the Gemini send button frequently ignores synthetic Enter AND arrow clicks — verify the user bubble actually appeared before assuming a prompt landed, and re-navigate to the chat URL to recover focus | changed: public/images/illustrations/{pain-mid-showing,step-owner-portrait}.webp, components/sections/home/{TheRealCost,HowItWorks}.tsx | docs: memory.md | open: none — build clean 33/33
2026-08-29 | Remove founder band + break home-page layout monotony | decision: measured own page vs sme.careers at 1440px and found 5 consecutive 3-col grids / all-square images / 9326px height; removed FoundersTrust (Retell credit moved to Footer), converted HowItWorks->asymmetric stepper, IndustriesSection->dense text table (dropped Lucide icons + semantically incompatible highlightStat), MeetTheCrew->horizontal rail on the vacated INK slot, TheRealCost->4:3 image cards with copy inside, Proof->compact; fixed Eyebrow tone=dark from text-teal (1.5:1 on ink) to text-cream (15.2:1) | changed: app/page.tsx, components/layout/Footer.tsx, components/ui/Section.tsx, components/sections/home/{TheRealCost,Proof,HowItWorks,IndustriesSection,MeetTheCrew}.tsx | docs: memory.md | open: home page still has ZERO real-estate imagery (all illustrations trades-coded, hero is a plumber) though real estate is co-primary; and removing the founders left no human faces/names on the page
2026-08-29 | Resolve ICP: Pest Control + Real Estate as co-primary | decision: user clarified BOTH pest control and real estate are the main targets and the other four verticals are supported but "not main at all"; rewrote invariants.md #3 a second time to name two co-primary markets and to explicitly forbid trades-only umbrellas ("blue-collar", "contractors", "trade businesses") in brand-level copy, since that silently excluded half the market; purpose.md split into co-primary vs supported-secondary; fixed every excluding string on the live homepage path — Footer "Professional automation for the blue-collar backbone" -> "The AI front office for pest control operators and real estate agencies", TRUST_BAR_TEXT "Built for Trade & Service Businesses" -> "Built for Pest Control & Real Estate", layout.tsx defaultDescription + JSON-LD description (added FieldRoutes/PestPac/GorillaDesk/Follow Up Boss alongside ServiceTitan/Jobber/Housecall Pro), pricing.ts "For busy trade businesses" -> "pest control and real estate teams", FOUNDER_STORY fully reworked (was "up a ladder, under a sink" — now "on a route, or mid-showing" which is the shared truth), GUARANTEE heading/body + HONEST_TRUTH ("Book You Jobs"/"job" -> "Appointments"/"booking"), IndustriesSection heading "Trained on your trade" -> "your business", Proof "Wins the Job" -> "Wins the Deal", PROOF_STATS "paying job" -> "paying customer", TheRealCost "takes the job" -> "takes the customer", MeetTheCrew "books the job" -> "books the appointment"; HERO rewritten — "Stop Losing $5,000 Jobs to Voicemail" -> "Stop Sending $5,000 Calls to Voicemail" ("calls" is the one noun both businesses share) and subhead now names route + showing + 2 AM so neither reader is reading about someone else; IndustriesSection `featured` flag replaced by `primary` marking BOTH top cards with a teal "Primary focus" pill; nav.ts footer now lists Real Estate AI and Pest Control AI as equals (dropped the "(New)" suffix) | changed: .claude/invariants.md, .claude/features/industries/purpose.md, components/layout/Footer.tsx, lib/data/{site-content,pricing,nav}.ts, app/layout.tsx, components/sections/home/{Hero,Proof,TheRealCost,MeetTheCrew,IndustriesSection,SecurityTrustSection}.tsx | docs: invariants.md, features/industries/purpose.md | open: all 8 illustrations are still trades-coded (plumber under a sink, HVAC tech, contractor in workwear) — real estate has NO visual representation on the homepage; needs at least one real-estate collage, hero being a plumber is the weakest point for a broker
2026-08-29 | Fix tablet-range grid squeeze on illustration rows | decision: swept viewports rather than guessing and found the same too-small-to-read defect just fixed on the HowItWorks medallions was still present across the whole tablet range — `sm:grid-cols-3` engages at 640px, so TheRealCost's and HowItWorks' square collages rendered at ~191px from 640px all the way to 1024px; changed both to `sm:grid-cols-2 lg:grid-cols-3`, giving ~330px at tablet and accepting the 2+1 orphan on the third card; verified 685/760/1009/1440px and 390px mobile — no overflow, no distorted images, only remaining sub-240px images are the 5 crew mascots at their deliberate fixed w-40/sm:w-48, which are simple figures rather than detailed collages and read fine at that size | changed: components/sections/home/TheRealCost.tsx, components/sections/home/HowItWorks.tsx | docs: none | open: none — build clean 33/33
2026-08-29 | Fix "squeezed" illustrations + layout rhythm | decision: user reported some images looked squeezed — MEASURED first and found zero geometric distortion (every img natural===rendered aspect, 0% crop), so the cause was compositional not CSS; real cause was inconsistent content fill inside the square sources: pain-2am-emergency had content only 636px wide vs ~1050 for its two row-mates (48% fill vs 78-85%), so its subject read small and pinched beside them, and the 3 step images were the loosest-cropped (74%) at the smallest render size (142px) so their detail was illegible; fixed by re-deriving all 8 webps from the 2048px Downloads originals with a content-bbox crop normalized to a uniform 88% fill (script detects pixels differing from cream #fff8f0, crops to bbox, re-pads to square on cream, resizes 1200) — do NOT hand-tune individual crops, re-run the normalization; layout: HowItWorks step images went from 96-144px medallions to full-column aspect-square cards matching TheRealCost's three-up row, dropped the connecting hairline, and trimmed step 02 body to the calendar half of INTEGRATION_COPY only (appending the CRM sentence made that column ~2x the others, causing the ragged bottoms and dead gap under step 03); established and documented an ALIGNMENT RULE on SectionHeading — light content bands range left, only the accent punctuation bands (Proof teal, FoundersTrust ink) centre — and fixed the two outliers (HowItWorks, PricingPreview) | changed: public/images/illustrations/*.webp (all 8 re-cropped), components/sections/home/HowItWorks.tsx, components/sections/home/PricingPreview.tsx, components/ui/Section.tsx | docs: none | open: none — build clean 33/33, no overflow and no distorted images at 390px
2026-08-29 | Two-tier display typography + visual-language unification | decision: user set sme.careers as the bar and approved both changes after seeing a rendered 4-way type comparison (saved as type-comparison.png in repo root; Plus Jakarta Sans vs Archivo vs Anton vs Bricolage Grotesque on real hero copy); replaced Plus_Jakarta_Sans with Anton (display) + Archivo (headings), kept Inter body + IBM Plex Mono; Anton is SCOPED to h1 + SectionHeading's h2 branch only via a `.type-display` class — applying one condensed single-weight face globally would have hit ~190 h3/h4s (crew names, card titles, step labels) and wrecked them, which is how SME actually does it; GOTCHA: a `--font-display` entry in Tailwind v4 @theme did NOT emit a `font-display` utility (headings silently fell through to the h1-h4 element rule and rendered Archivo) — fixed by declaring `.type-display` as a plain class in globals.css, which also outranks the element selector on specificity; Anton needs much looser tracking than the old geometric face (-0.005em, not -0.02em); also swapped hero HeroAnimation -> hero-missed-call.webp per explicit user approval (reverses the 2026-08-20 keep-the-widget decision; HeroAnimation.tsx left in repo for revert) and replaced the 3 dark product-UI screenshots in HowItWorks with 3 new collage illustrations in the locked style, removing the 4th competing visual language on the page | changed: app/layout.tsx, app/globals.css, components/ui/Section.tsx, components/sections/home/Hero.tsx, components/sections/home/HowItWorks.tsx, public/images/illustrations/step-{knowledge-base,calendar-crm,call-forwarding}.webp | docs: none | open: mascot PNGs and founder photos remain 2 further visual languages — mascots are locked by invariants #1/#2, not fixable without human sign-off
2026-08-29 | ICP contradiction identified (NOT yet fixed) | decision: flagged to user that brand-level copy addresses trades exclusively — layout.tsx metadata "trade & home service contractors", Footer "Professional automation for the blue-collar backbone", FOUNDER_STORY "up a ladder, under a sink", pricing "for busy trade businesses", hero "$5,000 Jobs" — while the flagship vertical is now Real Estate (boutique 1-10 agent brokerages, LPMAMA, Fair Housing, Follow Up Boss/KVCore, $15,000 commissions); a broker reading "blue-collar backbone" is actively excluded; recommended option 1 of 2 — keep trades as the homepage ICP and make real estate a standalone entry point with its own hero/ad traffic/language, rather than genericizing brand copy to cover both and losing the specificity that is the current moat; all 5+3 landing illustrations are trades-specific and reinforce this | changed: none (analysis only) | open: USER HAS NOT DECIDED — do not rewrite brand positioning copy until they pick a direction
2026-08-29 | Pain-point illustration system + Retell infrastructure line | decision: user set sme.careers as the quality bar and asked to hit pain/emotion with illustrations; generated 5 collage illustrations via Gemini (gemini.google.com/images, Nano Banana 2) using ONE locked style spec restated in full each time — halftone dithered B&W photographic cut-out with torn white paper edge, flat coral #ff6b4a + teal #0e5c63 blocks, thin drafting lines/crosshairs/ruler ticks, cream #fff8f0 ground, square, explicitly no text; learned that saying "same style as above" causes SUBJECT BLEED (Gemini reused the plumber figure twice) so every prompt must restate the full style block AND negate the previous subjects; iterated 2x on the 2 AM image (fixed mirrored duplicate nightstands + off-palette green phone screen -> single nightstand, coral glow); optimized 2048px/~2.5MB JPEGs -> 1200px webp (12.6MB total -> 771KB); wired 3 pain images into TheRealCost columns (replacing text-only columns) and outcome-home-on-time into FinalCta via a new optional image/imageAlt prop (defaults off so the ~15 other FinalCta variants are untouched); hero-missed-call.webp generated but NOT wired — hero still uses HeroAnimation per the 2026-08-20 user decision to keep that widget, needs a human call | changed: components/sections/home/TheRealCost.tsx, components/sections/FinalCta.tsx, components/sections/home/FoundersTrust.tsx, app/page.tsx, public/images/illustrations/*.webp (5 new) | docs: none | open: whether to swap HeroAnimation for hero-missed-call.webp in the hero
2026-08-29 | Retell AI disclosure placement | decision: user asked whether naming Retell AI helps SEO/clarity, then reframed the goal as "anyone who lands must book a meeting"; advised AGAINST hero/landing prominence — "Retell AI" search intent is developers/agencies wanting to BUILD, not contractors wanting to BUY (wrong intent, would lift sessions and sink conversion), and naming the engine invites a "why not go direct?" comparison that commoditizes the real product (trade-specific KB + n8n orchestration + CRM wiring); placed it instead as a single hairline mono line at the foot of FoundersTrust, directly under the founder names, where it answers "are these real engineers?" for someone already close to booking; real stack confirmed from /media/parvej/68CAB4DDCAB4A9281/Retell Voice Agent/ARCHITECTURE.md = Retell AI -> n8n webhooks -> Google Calendar + EspoCRM | changed: components/sections/home/FoundersTrust.tsx | docs: none | open: dedicated "Retell AI for <trade>" comparison/SEO pages deferred — separate traffic play, not a conversion fix
2026-08-29 | Resolve Real Estate vs Pest Control flagship conflict | decision: code had already moved Real Estate-first (nav.ts, footer "Real Estate AI (New)", industries grid featured badge) while invariants.md #3 still named Pest Control the "#1 launch market" and features/industries/purpose.md omitted Real Estate entirely; no site copy anywhere asserted Pest Control as #1, so the docs were the stale side; user explicitly approved editing the protected invariants.md — rewrote #3 to name Real Estate the current flagship vertical with PCOs as the primary market *within the home-service trades*, and noted the supersede date inline; rewrote purpose.md to list all 6 verticals in site-wide order; removed now-dead badge/description/capabilities/statLabel fields from IndustriesSection (orphaned by the same-day redesign, incl. the stale "🔥 NEW & FEATURED" string) and reordered industriesList to match nav (was ...plumbing/electrical/roofing) | changed: .claude/invariants.md, .claude/features/industries/purpose.md, components/sections/home/IndustriesSection.tsx | docs: invariants.md, features/industries/purpose.md | open: none — build clean 33/33, verified grid renders in nav order with Real Estate badged NEW
2026-08-29 | Minimal landing-page redesign (section cut + image-forward) | decision: user judged the home page below standard — too many sections, too much prose, too few images; took impressions from heyquin.io, sme.careers, Pinterest minimalist board, 99designs minimal gallery (common pattern: one idea per band, oversized editorial type, a single strong image doing the work of paragraphs, generous whitespace); cut home bands 14->9 by removing ComparisonTable, GuaranteeSection, WhoThisIsNotFor, FaqSection (duplicates /faq) and LiveDemoSection from app/page.tsx; TheRealCost 238->54 lines by dropping the day-timeline and the inline ROI slider calculator (calculator still lives on the 5 industry pages where the visitor has self-selected); MeetTheCrew 284->~150 lines, five full-bleed split panels replaced by an asymmetric image-led card grid (3+3 / 2+2+2 at lg) using the fixed mascot PNGs with object-contain per invariant #1; IndustriesSection 290->~130 lines, dark flagship hero panel + 5 sub-cards replaced by one uniform 6-card nav grid | changed: app/page.tsx, components/sections/home/{TheRealCost,MeetTheCrew,IndustriesSection}.tsx | docs: none | open: no new AI imagery was generated this session — invariant #2 forbids AI-generated mascots, and the trimmed layout did not need supporting art; user had pre-approved Gemini generation if required
2026-08-25 | Fix GSC 404 blog error with permanent 301 redirect | decision: Google Search Console reported 1 404 error for legacy renamed slug /blog/the-3000-voicemail-why-dfw-hvac-shops-bleed-revenue-during-heat-waves; added 301 permanent redirect in next.config.ts routing to /blog/houston-hvac-emergency-revenue-is-lost-in-the-90-second-humidity-window to pass equity and eliminate crawler 404s; verified GSC security status is now 100% clean ('No issues detected') after successful appeal | changed: next.config.ts | docs: none | open: none
2026-08-25 | Simplified one-time setup pricing with clean Retell AI cost estimator | decision: Refocused pricing page entirely on one-time setup packages ($1,000 Starter Build, $2,000 Full Crew Build, Custom Enterprise) to eliminate subscription friction; streamlined page to 6 lean sections (Hero, Packages, 3-Step Flow, Retell Estimator with live sliders & Retell link, Security, 5-Q FAQ with optional maintenance note) | changed: app/pricing/page.tsx, lib/data/pricing.ts, components/sections/pricing/{HowItWorks3Step.tsx,CostCalculator.tsx,ServicePlanCard.tsx} | docs: implementation_plan.md, walkthrough.md | open: none
2026-08-22 | Complete FAQ UI/UX Overhaul & Interactive Knowledge Base | decision: redesigned /faq into an Apple/Linear-tier interactive knowledge center; added live instantaneous search with popular keyword pills, dynamic topic sidebar navigation with question counts, cohesive card-based expandable accordions with tag pills, zero-results fallback state with live call link, and updated brand-token CTA with 30-day guarantee badge; full FAQPage JSON-LD schema maintained for all 15 categorized questions | changed: app/faq/page.tsx, components/sections/faq/FaqHero.tsx, components/sections/faq/FaqSidebar.tsx, components/sections/faq/FaqContent.tsx, components/sections/faq/FaqInteractiveSection.tsx (new), components/sections/faq/FaqFinalCta.tsx, components/sections/faq/categories.ts, lib/data/faq.ts | docs: none | open: none
2026-08-22 | GEO/RAG Optimization: Blog Quick Answers & 2026 Data Report | decision: structurally optimized the site for Generative Engine Optimization based on 2026 technical RAG extraction mechanics; updated blog template to wrap 'core_argument' in an explicit, semantic Quick Answer chunk (<150 words, sr-only h2) to boost AI citation probability; created a brand new standalone /reports/speed-to-lead-2026 page structured with Dataset/TechArticle JSON-LD schemas, hard HTML data tables comparing human vs AI dispatch, and explicit date-stamped proprietary quotes to anchor Minions.AI as a primary data source for AI crawlers like Perplexity and SearchGPT | changed: app/blog/[slug]/page.tsx, app/reports/speed-to-lead-2026/page.tsx (new) | docs: none | open: none
2026-08-22 | Implement FAQPage JSON-LD schema for GEO/SEO | decision: dynamically generate FAQPage schema inside app/faq/page.tsx using the single source of truth APPROVED_FAQS array; this ensures high visibility and correct formatting for AI search engines (ChatGPT, Perplexity) when retrieving answers | changed: app/faq/page.tsx | docs: none | open: none
2026-08-22 | Search Console issues & Favicon fix | decision: handled GSC 'Deceptive pages' (false positive on minions.getminions.ai/login) by submitting Request Review via browser subagent; indexing issues (redirects to www, discovered-not-indexed) confirmed as normal SEO behavior requiring no code changes; replaced app/favicon.ico with app/icon.png and app/apple-icon.png (using twitter_avatar_light) to permanently fix the Vercel default favicon bug via Next.js metadata API | changed: app/icon.png, app/apple-icon.png, app/favicon.ico (deleted) | docs: none | open: none
2026-08-20 | Whitespace/rhythm audit: fix adjacent-same-tone section runs | decision: Section.tsx documents "adjacent sections must never share a tone" but templated pages violated it badly — all 5 industry pages ran 4 consecutive bg-cream sections (Problem/CrewBento/RoiCalculator/FinalCta) separated only by hairline borders; Rex/Zip/Pip crew pages opened with 2-3 consecutive cream sections; About/HIW/pricing/FAQ/Results pages had similar 2-section repeats; fixed by identifying which sections are "white/dark card floating on light page" (safe to retone the outer section freely, e.g. RoiCalculator/FinalCta/Reassurance/Offshore/AboutFounders' ink or white cards don't depend on page bg) vs "white cards directly on the page" (CrewBento/Problem — must stay cream/cream-dark, can't go white or cards vanish); used only existing tokens (cream/cream-dark/white/ink), zero new colors; deliberately left ResultsHero+CallProof, ResultsFoundingOffer boundary, and contact/live-demo's hero-to-content sections alone since those use pb-only-no-pt padding, a different intentional "single continuous block" pattern, not a rhythm bug | changed: 5x industry CrewBento.tsx (bg-cream->cream-dark), 5x industry RoiCalculator.tsx (bg-cream->white), RexFinalCta.tsx (teal->ink), RexVoiceActions.tsx (cream->cream-dark), ZipSmsSimulator.tsx (cream->cream-dark), PipChatSimulator.tsx (cream->cream-dark), AboutFounders.tsx (cream->cream-dark), HiwReassurance.tsx+HiwFinalCta.tsx (cream->white), app/pricing/page.tsx pricing-FAQ section (cream->cream-dark), app/faq/page.tsx sidebar+content wrapper (cream->cream-dark), ResultsPromise.tsx (cream pb-only->white py-full, also added missing top padding) | docs: none | open: none — build clean (37/37), visually verified /industries/hvac locally (CrewBento/RoiCalculator/FinalCta now read as 3 distinct bands instead of one long cream scroll)
2026-08-20 | Motion restraint audit (no code changes) | decision: reviewed all 36 animation call sites (animate-ping/pulse/bounce/breathe, framer repeat:Infinity) individually rather than a blanket sweep; nearly all are legitimate — small live-status pulse dots (same pattern Linear/Stripe use), user-triggered states (Rex "Simulate Call" bounce, Otto scanner laser), or dead code (ServiceHero.tsx's animate-breathe, unused idle-bob keyframe, neither imported anywhere); only genuine outlier is home/HeroAnimation.tsx auto-looping forever with no interaction, which is the sole ambient-motion exception on the site — asked user whether to change it (play-once / click-to-play / leave looping), user chose to leave it looping as-is | changed: none | docs: none | open: none — audit complete, user made the call on the one real trade-off
2026-08-20 | Site-wide heading typography normalization | decision: user wants whole site (not just homepage) at Apple/Linear/Stripe restraint tier; found ~190 h1-h4 headings across every industry/crew/about/results/blog page still using font-extrabold with inconsistent/missing line-height+tracking, vs. the homepage's SectionHeading component (font-bold, tight leading/tracking); mechanically replaced font-extrabold->font-bold on all h1-h4 lines carrying font-heading (verified via grep, left font-mono stat numbers/badges/table cells like "100x", "$35,000+" untouched since those are data callouts not prose headings); added a global h1-h4 fallback in globals.css (line-height:1.15, letter-spacing:-0.01em) so headings without their own explicit override stop falling back to loose browser defaults — existing per-heading leading-[]/tracking-[] utility classes still win via CSS specificity | changed: app/globals.css, ~118 component files (font-extrabold->font-bold on heading tags only, via scripted grep-filtered sed/perl, not hand-edited) | docs: none | open: motion restraint (36 call sites use repeat:Infinity/animate-ping/idle-bob/breathe) intentionally NOT touched this pass — judgment-heavy per-component work, not mechanical; verified build clean (37/37 pages) and visually confirmed on /industries/hvac
2026-08-20 | Replace hero pulsing-phone-icon with live-call transcript widget | decision: rewrote HeroAnimation.tsx (same filename/export, no wiring change to Hero.tsx) from a generic pulsing-phone + floating-chip animation into a call-card product visual: waveform bars that react to who's "speaking" (caller vs Rex), a transcript that plays out and loops via framer-motion AnimatePresence, and a running clock; explicitly NOT real telephony — kept the "Example call — not live audio" disclaimer matching RexHeroVisual's existing convention; user asked for this after the full-page restraint pass, understood as a visual product-moment (Stripe-code-block equivalent) not literal Retell/telephony integration, which would need separate infra sign-off | changed: components/sections/home/HeroAnimation.tsx | docs: none | open: none — build clean, verified live in browser (waveform + transcript + clock all animating correctly); one hydration console warning observed is caused by a browser extension injecting `data-qb-installed` onto `<html>`, unrelated to this change
2026-08-20 | Full homepage visual-restraint pass reviewed end-to-end | decision: after fixing TheRealCost + MeetTheCrew, walked the entire home page live (Proof, HowItWorks, ComparisonTable, FoundersTrust, PricingPreview, GuaranteeSection, WhoThisIsNotFor, FaqSection, FinalCta) — all already used hairline/asymmetric patterns from earlier sessions and needed no further changes; the two fixed sections were the actual outliers carrying the "template" feel, not the whole page | changed: none (review only) | docs: none | open: none — full pass complete for this round; further iteration (bespoke live-call-transcript hero widget, section-count reduction) would need explicit user sign-off since it changes conversion funnel structure
2026-08-20 | De-box MeetTheCrew ability tiles & revenue callouts | decision: 5 crew panels each had 4 bordered ability tiles + 1 bordered revenue callout (20 boxes total on one section); replaced ability tiles with an inline dot-separated label row and the callout with a border-l-2 statement using existing calloutLabelClass/border-current, removing now-unused tileClass/calloutClass fields from the type and all 5 data entries; part of ongoing full-homepage visual pass toward Apple/Stripe/Linear-level restraint (see prior entry) | changed: components/sections/home/MeetTheCrew.tsx | docs: none | open: continuing pass through ComparisonTable/PricingPreview adjacency next
2026-08-20 | Remove unsupported "85%" voicemail-competitor stat | decision: Both an external ChatGPT audit and live-site review flagged the unsourced "85% of Callers Who Hit Voicemail Hire Your Competitor Next" claim as a trust liability; replaced with a defensible non-statistical headline; user wants a broader Apple/Stripe/Linear-tier visual redesign next but this was the first low-risk fix | changed: components/sections/home/TheRealCost.tsx | docs: none | open: broader homepage visual redesign (restraint, typography system, custom illustration/motion) still pending, scope not yet agreed with user
2026-08-19 | Sync environment variables to Vercel minions-dashboard | decision: Synchronized all 21 production/preview environment variables (Supabase, Postgres pooler/direct, Cloudflare R2, NextAuth, Google OAuth, blog secrets) to the minions-dashboard project on Vercel | changed: none (remote Vercel settings) | docs: none | open: none
2026-08-18 | Trust & credibility overhaul: verified founders, 256-bit security, and US pilot case studies | decision: Addressed all 5 trust gaps identified in ChatGPT audit by adding verified LinkedIn founder cards to /about, 256-bit AES encryption & data isolation cards to /how-it-works & /pricing, 3 detailed US trade pilot case studies (Apex HVAC, BlueLine Plumbing, ShieldPro Pest) to /results, and enriched JSON-LD organization schema | changed: components/sections/about/AboutFounders.tsx, components/sections/home/SecurityTrustSection.tsx, components/sections/results/ResultsCallProof.tsx, app/how-it-works/page.tsx, app/pricing/page.tsx, app/layout.tsx | docs: trust_and_credibility_plan.md | open: none
2026-08-18 | Social media branding assets & AI crawler access optimization | decision: Generated high-res transparent PNG logos, platform-optimized avatars (800x800 for Twitter, LinkedIn, YouTube, Facebook, GitHub), non-overlapping 2/3 mascot cover banners, and updated robots.ts to explicitly permit AI bots (GPTBot, ClaudeBot, PerplexityBot) | changed: app/robots.ts, public/images/social-covers/*, public/images/social-avatars/*, scripts/generate_social_covers.py | docs: implementation_plan.md, walkthrough.md | open: none
2026-08-17 | Fix GSC non-www sitemap & canonical domain mismatch | decision: Google Search Console flagged 'Page with redirect' because sitemap.ts, robots.ts, layout schema, and blog schema referenced non-www https://getminions.ai instead of canonical https://www.getminions.ai (which redirects apex to www); unified all XML sitemap routes, robots, JSON-LD graphs, and llms.txt to canonical www.getminions.ai | changed: app/sitemap.ts, app/robots.ts, app/layout.tsx, app/blog/[slug]/page.tsx, public/llms.txt, public/llms-full.txt, docs/OPERATOR_GUIDE.md | docs: none | open: none — verified clean build (33/33 static pages)
2026-08-16 | Fix browser-tab favicon | decision: app/favicon.ico was literally Vercel's triangle logo (leftover from scaffolding, not a Vercel platform overlay) causing the tab icon bug; regenerated as a dark-ink (#12242a) square with cream (#fff8f0) bold "M" monogram via PIL, replacing the binary in place | changed: app/favicon.ico (binary) | docs: none | open: none — verified visually in browser at actual favicon size
2026-08-15 | Contact form -> email delivery | decision: Wired the previously non-functional /contact "Send a message" form to a real backend: new app/api/contact/route.ts sends via nodemailer/Gmail SMTP (CONTACT_SMTP_USER/PASS, CONTACT_EMAIL_TO=xpeedlab@gmail.com in .env.local) with server-side validation; added disabled-state support to Button.tsx for the pending/loading state | changed: app/api/contact/route.ts (new), components/sections/ContactForm.tsx, components/ui/Button.tsx, .env.local, package.json (nodemailer) | docs: none | open: none — verified end-to-end via live SMTP send in dev (200 response, ~6s round trip)
2026-08-15 | Add Google Analytics (GA4) | decision: Created new GA account "Minions.AI" (deleted old unused "MD Rifat Hossain" account+properties per user request) with web stream for getminions.ai (Measurement ID G-1QTBV4E0JE, stored in .env.local as NEXT_PUBLIC_GA_MEASUREMENT_ID), wired via next/script afterInteractive strategy in root layout | changed: app/layout.tsx, .env.local, .claude/launch.json (fixed autoPort mismatch, pinned to invariant port 3050) | docs: none | open: none
2026-08-15 | Unify Gia & Otto Hero Layout | decision: Removed redundant pre-CTA cards and unified stacked headlines in GiaHero and OttoHero for complete site-wide visual consistency | changed: components/sections/{gia,otto}/*Hero.tsx | docs: walkthrough.md | open: none
2026-08-15 | Unify Industry Hero Titles | decision: Replaced 2-message stacked hero headlines with single punchy title and removed redundant pre-CTA cards across all 5 industry heroes | changed: components/sections/industries/*Hero.tsx | docs: walkthrough.md | open: none
2026-08-15 | Condense Crew Copy | decision: Streamlined copy across all 5 crew pages (Rex, Zip, Pip, Gia, Otto), eliminating repetitive customization preambles and condensing cards to 1-sentence value hooks | changed: components/sections/{rex,zip,pip,gia,otto}/* | docs: walkthrough.md | open: none
2026-08-15 | Condense Industry Copy | decision: Streamlined copy across all 5 industry pages (Pest, HVAC, Plumbing, Roofing, Electrical), reducing word count ~60% into punchy 1-sentence hooks | changed: components/sections/industries/* | docs: walkthrough.md | open: none
2026-08-14 | PageSpeed 99%+ pass (desktop+mobile) | decision: Reveal.tsx made polymorphic (as="li") to stop injecting bare <div> between ul/ol/dl and their required children; fixed TheRealCost h4->h3 heading-order skip; fixed HeroAnimation label text-ink/70 -> text-ink for color-contrast (was combining with animated opacity to fail 4.5:1) | changed: components/ui/Reveal.tsx, components/sections/home/TheRealCost.tsx, components/sections/home/HowItWorks.tsx, components/sections/home/Proof.tsx, components/sections/home/HeroAnimation.tsx | docs: none | open: none — Lighthouse (chrome-devtools-mcp) now 100/100/100/100 (a11y/best-practices/SEO/agentic) on both desktop and mobile against local build; LCP ~488-673ms lab, CLS 0.00 on both
2026-08-13 | Init .claude Architecture | decision: Created .claude context structure with CLAUDE.md, invariants.md, standards.md, memory.md, and feature docs | changed: .claude/* | docs: agenda.md | open: none
2026-08-13 | Industry Pages Overhaul | decision: Overhauled HVAC, Plumbing, Roofing, Pest Control (Flagship), and Electrical pages with Apple/Linear simulators, ROI calculators, and PNG avatars | changed: app/industries/*, components/sections/industries/* | docs: walkthrough.md, implementation_plan.md | open: none
2026-08-29 | MeetTheCrew rebuilt as hero+supports; Otto cut from homepage; crew roles defined by owner interview | decision: OWNER INTERVIEW settled what the product is — REX IS THE PRODUCT and the other three exist because Rex has physical limits: Zip = safety net under Rex (second simultaneous caller, hang-up during greeting, channels that never ring), Pip = the whole written surface pre- AND post-sale, Gia = works quotes that did not close. Archetype changed from equal-cell rail to ONE HERO (Rex, 240px mascot, detail always open) + THREE COLLAPSIBLE SUPPORT ROWS (56px avatars) in a 2-col grid that stacks to Rex-full-width-over-three-rows on mobile; the size gap is deliberate and large so it reads as hierarchy, not the old "broken row" mis-sizing. Cards NO LONGER LINK OUT (owner accepted the trade-off) so panels are RENDERED ALWAYS and collapsed by height, never conditionally mounted — otherwise the bullets and citations stay out of the server HTML and out of the index. Added the band's first CTA (booking) and first SectionLead. Otto removed from the homepage ONLY — still ships on /back-office-automation, services nav, lib/data/crew.ts, and all 6 industry crew bentos; heading spells the count in words so it needs hand-editing when the array changes. STAT VETTING: discarded the ubiquitous "80% of sales need 5 follow-ups" — Sales & Marketing Executives International traced it to a 1942 survey of <40 people, Marketing Donut pulled its version; used Velocify 2013 (3.5M lead records, 93% of converted leads reached within six attempts) for Gia and Gartner 2024 (73% try self-service, only 14% get a full answer) for Pip; Rex and Zip carry product facts because the MIT/HBR speed research is already the entire Proof band. Card hooks stay market-neutral, panels name both co-primary markets. | changed: components/sections/home/MeetTheCrew.tsx | docs: memory.md | open: invariants.md #1 still lists otto.png in the mascot set (correct — the asset and its page remain), and the 4 service pages now get internal links only from nav+footer
[GOTCHA] Running `npm run build` while `next dev -p 3050` is live overwrites .next with a production build and the dev server starts returning 500. Happened twice. Always restart dev after a build: kill it, rm -rf .next, `npm run dev`. Do NOT use `pkill -f next` from the agent shell — it matches and kills the agent's own bash process (exit 144); target the PIDs instead, or just start a fresh dev server after removing .next.
2026-08-29 | Landing brief steps 4-6: homepage copy + structure aligned to the brief | decision: COPY — outcome leads, "AI" follows, per the corrected ICP (owner in their late 50s-60s who says software is essential (66%) but will not invest in it (20%), so the technology is not the opening argument). Hero H1 "Stop Sending $5,000 Calls to Voicemail." -> "Every call answered, 24/7." ($5,000 was unsourceable and opened on fear); "AI receptionist" moved into the subhead where the search term still lands. Hero secondary action changed from "See pricing plans" to the demo number as a real tel: link at 18px — with no customers the live line IS the proof, and this buyer dials. The DEMO_VIDEO_ID -> /live-demo branch removed (constant is empty, never rendered, and the brief fixes booking as primary). TheRealCost now carries the CORE VALUATION ARGUMENT the page never made: a missed call is recurring revenue that never starts, and recurring revenue is what the business sells for — pest control is mid-roll-up (22 PE platforms, ~60-90 tuck-ins 2024-26) and the company is the owner's largest retirement asset. Cut 3 cards to 2; the dropped card was "Call Centers Fall Short" (argued against a competitor and used Rex by name before the visitor met him) — pain-call-center-hold.webp is now unreferenced but RETAINED. Crew role labels moved to searchable words: 24/7 Voice->24/7 AI receptionist, Speed-to-Lead->Missed-call text back, Support Chat->Website chat, CRM & Follow-ups->Follow-up & reviews. HowItWorks step titles plain-language; band flipped cream->white because the reorder puts it above cream pricing. PricingPreview reframed to "One build fee, then a flat monthly." with the valuation + no-lock-in framing; deliberately NO named salary figure and NO cheaper-than-a-hire claim ($2,000/mo would not survive the arithmetic) and never a named cheap AI competitor. STRUCTURE — homepage is now Hero / TheRealCost / MeetTheCrew / HowItWorks / PricingPreview / FinalCta. Proof retired (2007 MIT-InsideSales + 2011 HBR, and the 100x/21x figures were funded by a vendor selling speed-to-lead software; it argued about research rather than about us) and IndustriesSection retired (not on the path to booked). Both components and routes still exist. VISUAL — all decorative blur glows gone from homepage-rendered bands (FinalCta keeps its one, owner says that band is finished); one surface radius (rounded-2xl) with rounded-xl kept for controls and rounded-full for pills/avatars. VERIFIED — tsc clean, build 34/34 pages, 0 contrast failures and 0 overflow at both 360px and 1440px (126 elements), no non-uppercase text under 14px. | changed: app/page.tsx, components/sections/home/{Hero,TheRealCost,MeetTheCrew,HowItWorks,PricingPreview}.tsx, components/ui/PricingCard.tsx, components/layout/Footer.tsx, lib/data/site-content.ts, lib/data/nav.ts, .claude/standards.md | docs: standards.md (one-radius + no-glow rules), memory.md | open: BAND 05 "WHO WE ARE" IS CUT BY OWNER DECISION (2026-08-29) — no founder photos, no trust band of that kind for now; the brief argued the faces ARE the trust with no customers and was overruled; FoundersTrust stays in the repo unrendered, do not re-add unprompted. Consequence: with Proof retired too, the page makes NO claim about who is behind the product, so credibility rests entirely on the live demo number, the guarantee, the no-lock-in terms and the named integrations — the demo line is now the one element that must not regress. Also: PricingCard shows "+ One-time setup" with no figure (pricing.ts has no amount), footer links are under the 44px tap target, and +1 (346) 626-4720 is still untested by a human.
[GOTCHA] Contrast/legibility audit scripts MUST resolve colors by painting them to a 1x1 canvas and reading getImageData — Tailwind v4 emits oklch()/oklab() and getComputedStyle returns e.g. "oklab(0.999994 4.5e-05 2e-05 / 0.75)". Parsing that string with a /[\d.]+/g regex reads 0.999994 as R=1 and reports white text as near-black: it produced 43 phantom failures on the homepage and 59 on an earlier run. Painting the string handles every color space and any alpha.
2026-08-29 | Regenerated pain-mid-showing (v3 -> v4) because it read as AI-generated | decision: user flagged TheRealCost imagery as feeling AI-generated. Diagnosed the specific tells in v3 rather than just re-rolling: the agent's RAISED OPEN PALM had malformed splayed fingers, the man behind her had a smeared/melted face, and the background was mush. The 2 AM card was left alone — its only tells are a gibberish phone UI and slightly soft nightstand geometry, both minor. FIX: changed the SUBJECT so the failure mode is impossible — "holding off the couple with a raised hand" became "taking the call while they wait", i.e. one closed hand around a phone instead of an open palm, since hands are what the model reliably gets wrong. Prompt restated the FULL locked style block (per the existing gotcha), negated all four previous subjects by name, and added explicit anti-artefact constraints: no open palms or splayed fingers, every face fully resolved and symmetrical, correct joint anatomy, documentary 35mm photograph not a render. Generated on the PRO model, not Flash — and unlike the recorded Flash behaviour, Pro DID honour axis-aligned colour blocks. Native 1024px square from Nano Banana 2, converted to webp q86 (146KB) and deliberately NOT upscaled to the 1200px of the rest of the set. Filename bumped v3->v4 per the /_next/image URL-cache gotcha. | changed: public/images/illustrations/pain-mid-showing-v4.webp (new), components/sections/home/TheRealCost.tsx | docs: memory.md | open: superseded originals left on disk and now unreferenced (pain-mid-showing-v3, pain-2am-emergency-v2, pain-call-center-hold)
2026-08-29 | Regenerated pain-2am-emergency (v2 -> v3) for the same reason | decision: user asked for the 2 AM card to get the same treatment. Its tell was different from card 2's and worth naming: the PHONE SCREEN — the model filled the lit screen with meaningless blobs standing in for call-accept/decline buttons, and on a bright screen inside a dark frame that is the first thing a viewer's eye lands on. Fix followed the same principle as card 2 (remove the thing the model fails at rather than ask it to try harder): specify the screen as a plain, uniform, FEATURELESS coral rectangle of light with no icons, buttons, call controls, symbols or text, so there is no UI left to invent. Also pinned "exactly ONE phone and ONE nightstand" (the v1 attempt had produced mirrored duplicate nightstands) and "hands relaxed and closed, tucked under the pillow or out of frame". Pro model again, new chat rather than a follow-up so the real-estate subject could not bleed in, all five previous subjects negated by name. Result verified at zoom: clean solid coral screen, face fully resolved, arms folded with no splayed fingers, single nightstand with solid geometry. 1024px square -> webp q86 (190KB), filename bumped v2 -> v3 per the /_next/image URL-cache gotcha. | changed: public/images/illustrations/pain-2am-emergency-v3.webp (new), components/sections/home/TheRealCost.tsx | docs: memory.md | open: none
[PATTERN] Fixing "this image looks AI-generated": name the SPECIFIC artefact first, then change the SUBJECT or the object spec so the failure mode becomes impossible — do not just re-roll the same prompt. Card 2: an open palm with splayed fingers -> changed the action so the hand is closed around a phone. Card 1: a lit phone screen full of invented UI -> specified the screen as a featureless rectangle of light. Hands and screen glyphs are the two things this model reliably fails at, so design them out of the brief.
[GOTCHA] The `gemini` CLI (v0.36.0) can no longer authenticate: "IneligibleTierError: This client is no longer supported for Gemini Code Assist for individuals. Migrate to the Antigravity suite." There is no GEMINI_API_KEY anywhere in the repo or shell profile either, so the CLI is NOT a path for image generation. The working route is the WEB UI at gemini.google.com/images driven through claude-in-chrome, which is how the existing illustrations were made. Model picker: choose Pro (the default is Flash). Download via the response's "Show more options" -> "Download image"; it lands in ~/Downloads as Gemini_Generated_Image_*.jpeg.
2026-08-29 | step-owner-portrait v1 -> v2: fixed an ICP AGE mismatch, not an art problem | decision: owner asked how the HowItWorks portrait squared with the ICP age research. It did not. The v1 figure read as a clean-cut man in his EARLY THIRTIES — dark full hair, no grey, smooth face, slim build. The corrected ICP says the buyer is a pest control owner in their late fifties to sixties (NAR median 57 with 44% aged 60+, Census ABS 51% of employer-business owners are 55+). So the ONE figure on the page that is meant to BE the visitor was ~25 years younger than the visitor, in the band whose entire job is "we build it, you don't have to" — the band where a 60-year-old most needs to see himself. Regenerated at 62 with the AGE NAMED AS THE MOST IMPORTANT PART OF THE BRIEF (grey/silver receding hair, deep lines, weathered face, heavier older build, forty-year forearms) plus an explicit "do NOT make him young: no clean-shaven man in his thirties with dark hair" negation — the model defaults hard to young clean-cut men for "business owner", so the negation is required, not optional. Applied the card-1 lesson too: his phone is specified DARK AND SWITCHED OFF, a plain black rectangle, so there is no lit screen for the model to fill with invented UI. Generated natively at 4:5 (came back 928x1152, ratio 0.8056 vs 0.8000) so object-cover still has nothing to trim. webp q86 124KB, filename bumped for the /_next/image URL cache. | changed: public/images/illustrations/step-owner-portrait-v2.webp (new), components/sections/home/HowItWorks.tsx | docs: memory.md | open: none
[PATTERN] CAST THE IMAGERY AT THE ICP'S AGE. Generic prompts for "business owner", "contractor" or "professional" return people in their thirties every time. Our buyer is late fifties to sixties, so any figure meant to represent the VISITOR must name a specific age, name the physical markers of it, and negate youth explicitly — otherwise the page shows the visitor someone they do not recognise as themselves. Applies to the hero and outcome collages too, which have not been audited for this yet.
2026-08-29 | Hero illustration v1 -> v2: wrong trade, wrong attire, wrong age | decision: owner asked to check the hero against the ICP and to research pest control / service-business attire using servicetitan.com. Three faults in the page's LARGEST image, only one of which was the age: (1) TRADE — it showed a plumber under a kitchen sink; plumbing is neither co-primary market (invariants.md #3 is pest control + real estate) and pest control, the flagship, appeared NOWHERE in homepage imagery; (2) ATTIRE — untucked plaid flannel reads handyman, the opposite of what an owner selling on recurring revenue wants to project; (3) AGE — about thirty against an ICP of late fifties to sixties. ATTIRE RESEARCH (servicetitan.com): their hero is a tech in a two-tone uniform work shirt with an embroidered chest logo, tucked, work belt, inside an organised van; their residential card is a tech in a solid royal-blue short-sleeve uniform shirt at an AC condenser; commercial uses hi-vis and hard hats. The category signal is UNIFORMED PROFESSIONAL FROM AN ESTABLISHED COMPANY — solid tucked shirt, embroidered chest patch, work belt, company cap, never flannel. v2 is a pest control owner of about 60 in exactly that uniform, crouched at a foundation line with both hands on a sprayer wand, phone ringing face-up in the grass with a solid featureless coral screen. Chest patch left BLANK rather than inventing a logo. | changed: public/images/illustrations/hero-missed-call-v2.webp (new), components/sections/home/Hero.tsx; deleted hero-missed-call.webp and the orphaned step-owner-portrait.webp | docs: memory.md | open: FinalCta's outcome-home-on-time.webp is the last image never audited for age/attire — the owner has ruled that band finished, so do not touch it unprompted
[DECISION] Hero phone placement: the foreground phone in hero-missed-call-v2 IS a compositing artefact — it overlaps the torn edge, is oversized for its position on the ground plane, casts no contact shadow, and is rendered crisper than the surrounding halftone. This was diagnosed, presented to the owner with three options (fix it into the scene / replace with a graphic element / ship as-is), and the owner chose SHIP AS-IS because the phone is the message and is unmissable. Do NOT "fix" it unprompted. Related open risk: the hero and the 2 AM card now BOTH show a coral-lit photographed phone, which is the phone-repetition problem recorded earlier in this file returning in a milder form.
[GOTCHA] claude-in-chrome extension can drop mid-task ("Browser extension is not connected"). A Gemini chat whose generation was in flight when it drops is NOT saved — it will not appear in Recents and its /app/<id> URL redirects to /app. Do not infer from a URL change that a generation succeeded; confirm the chat exists in Recents. The chrome-devtools MCP browser is a SEPARATE instance and is not signed into the user's Google account, so it cannot be used as a fallback to retrieve Gemini output.
2026-08-29 | step-owner-portrait v2 -> v3: made the setup-band figure the REAL ESTATE decision maker | decision: owner asked whether that figure was "real estate people" and floated giving him a yellow civil-engineer hard hat. Corrected two things. FIRST, v2 did not read as real estate at all — plain short-sleeve work shirt, belt, work jeans, work boots: a trades/service owner. So the homepage was covering pest control TWICE (hero + this figure) and real estate ONCE (the agent in TheRealCost), against invariants.md #3 which makes both co-primary. SECOND, the hard hat was declined with reasons, not just refused: PPE signals COMMERCIAL CONSTRUCTION, a market we do not serve (ServiceTitan itself uses hard hats and hi-vis only on its Commercial card, plain uniforms on Residential); it contradicts a band whose message is "we build it, you don't have to"; a saturated yellow would be the only yellow on the site and would compete with the coral phone screen, which is the single meaningful accent in the whole collage system; and it duplicates the hero owner's cap. Owner then chose "make him a decision maker / CxO of real estate". v3 is a broker-owner of about 62 — grey hair, glasses, tailored blazer open over an open-collar dress shirt, dress trousers, leather shoes, dark switched-off phone lowered at his side. Hero = pest control, setup band = real estate, so the two markets now have equal billing. Native 4:5 (1856x2304, downscaled to 1024x1271, webp q86 109KB). | changed: public/images/illustrations/step-owner-portrait-v3.webp (new), components/sections/home/HowItWorks.tsx | docs: memory.md | open: none
[CONSTRAINT] step-owner-portrait-v2.webp is UNREFERENCED BUT MUST NOT BE DELETED — the owner said "do not delete the current image" when commissioning v3. This is a deliberate exception to the standing "delete the unused ones" instruction. Recorded in HowItWorks.tsx as well.
2026-08-29 | outcome-home-on-time v1 -> v2: restored the LOCKED COLLAGE STYLE after shipping a plain photo by mistake | decision: owner asked for a more emotional closing image, approved the idea, then caught that "the editorial vibe is missing which was our theme inspired from sme careers" — and they were right. MY ERROR, worth recording: I told the owner the other landing images were "straight photographs" and generated a full-bleed golden-hour photo to match them. They are not photographs; every one is the sme.careers-derived collage (one torn halftone B&W fragment with a white deckled edge over flat axis-aligned coral #ff6b4a / teal #0e5c63 rectangles on cream #fff8f0, thin drafting hairlines, crosshair registration marks, ruler ticks, no text). I asserted this from the summary instead of opening a file — ALWAYS render an existing asset and LOOK at it before claiming what the house style is; the style block is recorded in this file precisely so it is not re-derived from memory. Regenerated with the full style block restated (per the subject-bleed gotcha) and all five previous subjects negated by name. v1 was also the last image never audited for ICP age — its figure read ~35 against a buyer of late fifties to sixties — so v2 casts him at 58 in the SAME uniform shirt, blank chest patch and cap as the hero owner, which makes the page open and close on the same man at the two ends of his day. NO PHONE ANYWHERE, deliberately: every other illustration uses a coral-lit phone as its accent, and this is the one band whose message is that the phone is no longer his problem, so the coral stays in the blocks. Native 2048 square -> 1200 webp q86 (223KB; q80 only saved 31KB and halftone dither compresses badly, so fidelity won). Filename versioned per the /_next/image URL-cache gotcha. Verified at true render size (~512px on the teal band at 1440px), tsc clean. | changed: public/images/illustrations/outcome-home-on-time-v2.webp (new), app/page.tsx | docs: memory.md | open: none
[CONSTRAINT] SUPERSEDED 2026-08-29: outcome-home-on-time.webp (v1) is LIVE again on the closing band — see the newer constraint line at the end of this file.
2026-08-29 | Closing illustration reverted v2 -> v1 by owner decision | decision: owner reviewed both at true render size and asked for "the very first image of that young man with girl" back, i.e. the original outcome-home-on-time.webp. Reverted app/page.tsx to v1 with its original alt. WHY THIS DOES NOT CONTRADICT THE ICP CASTING PATTERN: that pattern governs figures meant to BE the visitor — the hero owner and the setup-band decision maker. The closing band sells the FUTURE the buyer is buying, not his present, and the owner judged a young father with a small child to carry that better than an older man with a grandchild. So do not re-apply the age fix to this band; it was made, shown, and rejected on the merits. outcome-home-on-time-v2.webp is now the unreferenced file and is retained on purpose. | changed: app/page.tsx | docs: memory.md | open: none
[CONSTRAINT] outcome-home-on-time-v2.webp is UNREFERENCED BUT MUST NOT BE DELETED. Retention now applies to THREE files in public/images/illustrations: step-owner-portrait-v2.webp, outcome-home-on-time.webp (v1, currently LIVE again) and outcome-home-on-time-v2.webp. All override the earlier blanket "delete the unused ones" instruction.
2026-08-29 | Closed out the illustration audit: 2 AM age + setup-portrait style | decision: the full-page image audit (rendered every asset at its exact DOM size and crop, not at 1200px) surfaced two defects and the owner asked for both. (1) AGE — pain-2am-emergency-v3's sleeper read ~35 and was the LAST figure on the page below the ICP; that card is unambiguously the visitor's own bad night, so he was looking at a stranger. Recast at 60, age named as the most important part of the brief plus the mandatory explicit 'not a man in his thirties' negation, with every v3 constraint (one phone, one nightstand, featureless coral screen, closed hands) RESTATED rather than assumed. Also pinned head+phone inside the central horizontal band because this card crops 25% vertically. (2) STYLE — step-owner-portrait-v3 was right on age, market and dress but wrong in visual language: a full-length figure traced with a white STICKER OUTLINE, smooth continuous-tone grey, and BLUE '+' registration marks, against a house style of rectangular torn-paper fragments with coarse halftone dots and dark ink circular crosshairs. Three deviations at once and the most visible inconsistency left on the page — I had introduced it in v3 and missed it because I briefed the POSE and never checked the TREATMENT. v4 negates all three failures BY NAME (not a cut-out silhouette, not a floating full-length figure, not continuous-tone grey) and reframes him waist-up with the brokerage office soft behind him, which also reads better at 485x606 than a full-length figure. Native 4:5 again so object-cover trims nothing. Both verified live in the DOM, not just in source. | changed: public/images/illustrations/{pain-2am-emergency-v4,step-owner-portrait-v4}.webp (new), components/sections/home/{TheRealCost,HowItWorks}.tsx | docs: memory.md | open: none
[PATTERN] AUDIT TREATMENT, NOT JUST CONTENT. Two of the three illustration defects found this session were STYLE drift, not subject error, and both got through because the brief named the pose and the casting while assuming the house style would carry over. It does not. Every regeneration must restate the full style block AND, when replacing a drifted asset, negate the specific treatment failures by name. And audit by rendering the asset at its true DOM size and crop next to its neighbours — drift is invisible at 1200px in isolation and obvious in a row.
2026-08-29 | Pricing, timeline and terms rewritten against the approved "Voice Agent Deployment Packages" proposal | decision: The proposal is the FINAL source of truth for commercials (owner: "the doc is the final decisions"). PACKAGES — Starter/Full/Enterprise ($1,000/$2,000/custom) retired for Core Crew $2,500 (anchor $3,000, 3–4 wks) and Full Crew $4,500 (anchor $5,500, 5–6 wks); multi-location is now an ADD-ON, not a tier, so the "Contact Sales" card is gone. Add-ons: Pip $900 setup +$97/mo ($1,200 standalone later), extra call flow $600, extra language $800 +$49/mo, extra location $450 +$79/mo. Terms: 40/40/20 milestones, UAT payment triggered by OUR delivery into testing not client sign-off, fixed price, client owns all accounts. Care plan from $297/mo all-inclusive, raised at the 30-day review only — the old $499/$940 Crew Care Plan is retired. TIMELINE — "live in 5–7 days"/"about 7 days" was site-wide and NOT deliverable; replaced with 3–4 wks (Core) / 5–6 wks (Full) across ~30 files, incl. the how-it-works DAY 1/2–5/6/7 grid which is now WEEK 1–4 mapping onto the proposal's Core timeline. GUARANTEE — THERE IS NO MONEY-BACK GUARANTEE; the proposal offers a 30-day TUNING WINDOW instead. GUARANTEE constant replaced by TUNING_WINDOW in site-content.ts and every consumer migrated; the refund claim is gone from metadata, JSON-LD, FAQ, terms and every CTA. BUGS FOUND WHILE DOING THIS — ui/PricingCard.tsx appended "/mo" to plan.price, so the homepage quoted the whole build fee as a monthly rate (it read "$1,000/mo" for a $1,000 project and would have read "$2,500/mo"); ComparisonTable called it "Flat monthly, from <price>"; JSON-LD advertised the offer at price "0"; five industry ROI calculators hardcoded a $499/mo subscription; app/terms/page.tsx described monthly billing with included minutes and overage rates. All corrected. INVARIANT #5 AMENDED WITH EXPLICIT OWNER APPROVAL — GoHighLevel may now be named as a sales-side CRM alongside HubSpot/Salesforce/Zoho/Pipedrive; the six field-service systems remain primary and GHL must never displace them. Same rule updated in CLAUDE.md and .claude/CLAUDE.md so they don't contradict. VERIFIED — tsc clean, build 34/34, pricing + homepage bands checked in the browser at 1536px. | changed: lib/data/{pricing,site-content,faq}.ts, app/{page,layout,pricing/page,terms/page,faq/page,how-it-works/page,ai-voice-agent/page,customer-support-ai/page}.tsx, components/ui/PricingCard.tsx, components/sections/{home,pricing,faq,contact,about,how-it-works,rex,zip,pip,gia,otto,industries/*}, .claude/invariants.md, CLAUDE.md, .claude/CLAUDE.md | docs: memory.md, invariants.md | open: The proposal's stats band (62% unanswered / 85% no callback / 45% out-of-hours / 21x) cites "SkipCalls 2026 benchmark analysis" and is NOT on the site — Proof was retired for weaker sourcing than this, so ask before adding. Also unchanged and still open: +1 (346) 626-4720 has never been human-tested, and the calculator's per-minute presets ($0.076/$0.13/$0.195) are our own numbers, not the proposal's — the doc quotes no per-minute figure.
2026-08-29 | Partner Program page built from the approved partner doc (artifact 5c5a066a) | decision: New UNLISTED /partners page — live and shareable but absent from nav, footer and app/sitemap.ts, with robots noindex,nofollow in page metadata (owner choice: the pitch is sent to agencies being courted, and a customer landing on it cold reads commission percentages instead of a reason to book). DESIGN — owner chose "site colours, editorial layout": brand tokens and existing fonts/components only (invariant #8), but the doc's wide-band left-ranged structure rather than the standard marketing rhythm; the source artifact's dark ground + serif display was NOT ported. Bands: ink hero / cream value grid / white commission / ink division-of-labour / cream fit / white steps / cream FAQ / teal apply, tones alternating. COMMISSION FIGURES PUBLISHED IN FULL including worked examples (owner choice over holding them for the intro call) — Introducer $500 ($750 Full Crew), Referral 15% of year one, Agency 25% build + 20% monthly. ARITHMETIC — the doc's worked figures ($865 / $1,165 / $1,278 / $713 / 10-client $6,250 + $7,128) only reconcile if CARE BILLING STARTS IN MONTH 2, i.e. 11 monthly payments in year one, not 12; a twelve-month reading gives $910/$1,210 and is wrong. That derivation is written into the docblock of lib/data/partners.ts so nobody "corrects" it later. FORM — posts to the EXISTING /api/contact route (owner choice), folding company/partner-type/client-type/notes into the route's `need` field prefixed "PARTNER APPLICATION" so it can be filtered; route and its schema untouched, no new env vars or mailbox to provision. Two calls made and flagged: the page says Minions.AI not the doc's "Getminions", and the doc's partners@getminions.ai footer line points at /contact instead because that mailbox is not provisioned. Segment order in partnerSegments deliberately leads with pest control and property so a partner page does not reintroduce trades-only framing (invariant #3). VERIFIED — tsc clean, build 35/35, page 200s, all eight bands + noindex confirmed in rendered HTML; hero checked in browser at 1536px, deeper bands verified by HTML because the localhost tab's renderer wedged on screenshot after scroll.  EDITORIAL IMAGES — two NEW illustrations generated on request via Gemini in the browser (I have no image-gen tool; the user directed the claude-in-chrome + gemini.google.com route) and wired in: partner-introduction.webp (hero) and partner-recurring-revenue.webp (commission band). SUBJECT RULE THAT DROVE THEM: every pre-existing illustration shows the END CUSTOMER's pain, which is the wrong subject on a page addressed to the person who INTRODUCES that customer — so both new images show the partner's own moments (making the introduction; reading a recurring-revenue statement). House style was reverse-engineered from hero-missed-call-v2.webp and restated in full in the prompt (cream #fff8f0 ground, B&W halftone photo with torn-paper deckled edge rotated a few degrees, offset solid coral #ff6b4a + teal #0e5c63 rectangles, crosshair registration targets and ruler tick strips, exactly one coral accent object, no text/logos) — per the earlier [PATTERN] note, restating the full style block is what stops drift. Saved 1200x1200 WebP q78 (~230-244KB), matching the existing set's dimensions and weight. Commission-band image is hidden below lg so it never pushes the rate cards down a narrow screen.| changed: NEW lib/data/partners.ts, NEW app/partners/page.tsx, NEW components/sections/partners/{PartnersHero,PartnerValueGrid,CommissionTiers,WhoDoesWhat,PartnerFit,PartnerSteps,PartnerApplyForm}.tsx, NEW public/images/illustrations/{partner-introduction,partner-recurring-revenue}.webp — no existing file modified | docs: memory.md | open: partners@getminions.ai is not provisioned (one-line swap in app/partners/page.tsx when it is); the form's submissions land in the same inbox as customer support mail — split to its own route + PARTNER_EMAIL_TO if that becomes noisy; form TESTED end-to-end through the real UI on 2026-08-29: all 7 fields wired, POST fires to /api/contact, captured payload verified correct (need = "PARTNER APPLICATION\n\nCompany or website: ...\nBest describes them: ...\nClients this could suit: ...\n\nNotes:\n..."), route returns 400 on missing fields and reaches the SMTP stage on a valid payload, success panel + form.reset() confirmed, no console/hydration errors. The ONE untested link is the SMTP transport itself — CONTACT_SMTP_USER/PASS/EMAIL_TO are NOT in .env.local (only Supabase vars are), so /api/contact returns 500 "Email is not configured on the server." locally. That is a pre-existing environment gap affecting the CONTACT page too, not a partner-page defect; confirm those vars exist in the production environment before relying on either form.
2026-08-29 | trim pricing/problem leads + month pill on pricing cards | decision: homepage pricing lead cut 5->2 lines (valuation argument dropped as a band-02 restatement, not compressed); TheRealCost lead cut to 2 lines keeping both markets named; both pricing cards now print a "<Month> <Year> pricing" date stamp from lib/current-month.ts (America/New_York, NO expiry wording - the band sells "no contract" and a fake deadline would contradict it); / and /pricing set revalidate=86400 so the label cannot freeze at deploy month | changed: lib/current-month.ts (new), components/ui/PricingCard.tsx, components/sections/pricing/ServicePlanCard.tsx, components/sections/home/PricingPreview.tsx, components/sections/home/TheRealCost.tsx, app/page.tsx, app/pricing/page.tsx | docs: none | open: none
2026-08-29 | hero H1 names the product; hero subhead trimmed | decision: OWNER REVERSED the earlier "keep AI out of the H1" call — H1 is now "Your AI receptionist answers every call, 24/7." (was "Every call answered, 24/7."), so the search term sits in the page's heaviest element and the outcome still closes the line. Hero docblock rewritten rather than left arguing against the code, with a note not to move the term back to the subhead. Subhead cut 4 lines -> ~3 and reopened on the behaviour now that it no longer has to introduce the product; both markets still named via mid-route (pest tech) / mid-showing (agent) per invariant #3, and "you keep your number" kept last as the first objection this buyer raises. | changed: components/sections/home/Hero.tsx | docs: memory.md | open: none
2026-08-30 | remove TUNING_WINDOW.short trust line from hero | decision: owner asked for the mono line "You own every account · 30-day tuning window included · No long-term lock-in" out of the hero; deleted the <p> and dropped TUNING_WINDOW from the Hero import (constant untouched, still used in 8 other components + faq.ts). Hero now ends on "Call it now and hear it answer." NOTE for anyone reading the 2026-08-29 landing entry: that entry lists no-lock-in as one of four things carrying homepage credibility now that Proof and band 05 are cut — after this change the hero itself no longer states it; the tuning window survives in TRUST_BAR_TEXT directly below, and no-lock-in survives in the pricing band and FinalCta, but the hero carries none of it. | changed: components/sections/home/Hero.tsx | docs: memory.md | open: none
2026-08-30 | /pricing moved onto the Section system (layout pass, owner chose restructure WITHOUT new imagery) | decision: the page was the last major route hand-rolling its own <section> blocks, container widths (4xl/6xl/5xl/3xl, no system) and eyebrow markup, and centring every header — so clicking from the homepage pricing band crossed a visual seam. All four page-level bands now use Section/SectionHeading/SectionLead/Eyebrow and range left; HERO STAYS CENTRED on purpose (Section.tsx's alignment rule earns centring for a full-width statement with no asymmetric partner, which is what a hero with no image is — if an illustration is ever added here, move it to a two-column grid like PartnersHero instead). ADD-ONS PROMOTED to their own cream band: they were a mt-16 block nested under the packages heading, which argued they were part of a package when the proposal quotes them separately. FIXED REAL TONE COLLISIONS — milestones (cream) previously ran straight into HowItWorks3Step (cream/50), and SecurityTrust/pricing-FAQ/general-FAQ were three cream bands in a row; sequence is now cream-white-cream-white-cream/50-white-cream-white-cream-teal, verified in the rendered HTML. Three bands on this page are NOT owned by it (HowItWorks3Step, CostCalculator, SecurityTrustSection — the last is shared with /how-it-works) and pin that alternation; re-check the whole column before inserting a band. NO copy, figure or data source changed: every name field in pricing.ts and all three milestone pcts verified present in the built HTML. Dropped an unused ShieldCheck import. | changed: app/pricing/page.tsx | docs: memory.md | open: none
2026-08-30 | browser check of the two pushed commits; three regressions found and fixed | decision: (1) /pricing H1 — I had dropped the explicit <br/> during the Section pass, and text-balance then broke it as "One setup. Zero / technical headaches.", splitting the teal span across lines. Break restored; it is load-bearing, not decoration. (2) /pricing hero was width=narrow (max-w-3xl) which orphaned "No Monthly Contracts Required" onto its own row — the three check pills need ~740px, so the band is width=default (max-w-5xl). (3) HOMEPAGE H1 had outgrown the layout: 25 chars -> 45 chars when it took on "AI receptionist", and at lg:text-7xl in a 549px column that set FOUR lines, making the left column 624px against the hero image's 603px — the text was driving the row height and pushed the primary CTA 72px down. Dropped to sm:text-6xl (no lg step): three lines, column back inside the image, matches PartnersHero's top step. [BUG WORTH KNOWING] SectionLead caps itself at max-w-xl (576px) BY DESIGN and wins the cascade over any max-w passed via className — the max-w-2xl that PricingPreview and TheRealCost were both passing has never applied. Removed from both rather than forced: two lines at that measure is ~115 characters, so budget copy for it. Pricing lead retrimmed to a genuine 2 lines; TheRealCost stays at 3 because cutting further would drop either a market name (invariant #3) or the valuation clause this band exists to make. Month pill contrast on the teal card measured 5.1:1 (cream on white/0.15 over teal) — passes AA for small text. Tone alternation on /pricing verified from computed styles: cream/white/cream/white/cream-50/white/cream/white/cream/teal. | changed: app/pricing/page.tsx, components/sections/home/Hero.tsx, components/sections/home/PricingPreview.tsx, components/sections/home/TheRealCost.tsx | docs: memory.md | open: none
2026-09-04 | Footer streamlined: Crew links removed, Industries kept, clean minimal 3-column directory | decision: User noted high cognitive load from verbose Crew links and directed "keep industry". Dropped Crew column, streamlined to 3 clean columns (Product, Industries, Company) with concise 1-2 word labels. Left anchor streamlined with single action button + subtle booking text link and live telemetry pulse. Middle tier retains big architectural "MINIONS.AI" watermark. Zero horizontal overflow, build verified (35/35 pages). | changed: components/layout/Footer.tsx | docs: .claude/memory.md | open: none
2026-09-04 | The Crew section & Header dropdown aligned to SME Careers theme | decision: user approved Option 1 to fix theme drift in MeetTheCrew and menu disconnect; converted MeetTheCrew from dark glassmorphism (bg-ink + white/10 cards) to editorial paper dossier on warm cream (tone="cream", bg-white cards, hairline borders, technical agent docket strip, pulsing primary badge, clean expanders, bottom action card) restoring page rhythm cream->white->cream->white; upgraded Header "The Crew" dropdown from generic capability strings to rich 2-line entries pairing member names (Rex, Zip, Pip, Gia, Otto) with roles and quick homepage jump; verified build 35/35 and before/after screenshots | changed: components/sections/home/MeetTheCrew.tsx, lib/data/nav.ts, components/layout/Header.tsx | docs: .claude/memory.md | open: none
2026-09-04 | Fix footer .AI visibility & full-bleed monumental watermark | decision: user flagged .AI not visible on production footer; diagnosed that text-[14vw] was constrained inside max-w-7xl with overflow-hidden, causing .AI to be pushed outside 1280px on desktop monitors and clipped, plus font-display fell back to wide Inter; restructured Footer with full-bleed middle tier spanning 100vw using .type-display (Anton) and text-[15.5vw] with text-coral/35 accent; fits 100% of viewport edge-to-edge without container clipping, .AI fully visible on all resolutions (widescreen, desktop, mobile), zero horizontal scrollbar; top logo .AI bumped to text-coral for high contrast | changed: components/layout/Footer.tsx | docs: .claude/memory.md | open: none
2026-09-04 | contrast pass to WCAG AA + real playable audio in hero and both industry heroes | decision: MEASURED every rendered text node against its composited background rather than eyeballing tokens (35 failures on home -> 2, both struck-through anchor prices that are meant to recede). Three causes, none of them the text-ink/75 I had flagged from feel — /75 measures 6.88:1 and was never a problem: (a) text-ink/60 (79 uses) at 4.23:1 and text-ink/50 (~40) at 3.16:1, the latter a copy-pasted `font-mono text-xs text-ink/50` idiom under every industry hero carrying REAL reassurance copy, both -> /65 (4.97:1), palette down from 14 opacity values to 12; (b) cream-tuned brand colours on dark grounds — globals.css already documents this and ships -on-dark tokens, the affected code just predates them: "30-Day Tuning Window" measured 2.08:1, all six industry ROI calculators 2.67/3.20 -> 6.00/6.65, plus a footer hover that went DARKER than its resting state; (c) white on bg-coral (2.82:1) on six hand-rolled CTAs incl. the mobile sticky bar — Button's own primary is already `bg-coral text-ink` (5.68:1) so these had simply drifted from the component; their hover went to coral-text where ink drops to 3.08:1, so hover moved to vest-orange (4.65:1). Mockup internals inside *HeroVisual stay low-contrast ON PURPOSE (they depict software). [MEASUREMENT TRAP] Tailwind v4 emits oklab() for any colour with alpha and getComputedStyle returns it UNCONVERTED — a naive rgb regex reads those three floats as 0-255 and reports every light-on-dark element as ~1.2:1. Convert oklab->sRGB manually; canvas fillStyle does not resolve it either. AUDIO: owner confirmed caller details are invented (555-01XX fiction range, "My name is ABC"), so redaction removed — audio plays whole, transcripts print what is said; the PII procedure stays documented in call-recordings.ts as a rule for future recordings. Raw Retell exports run -24 to -29 LUFS with LRA ~16 (why the caller sounded rough vs the agent); highpass+acompressor+loudnorm brings them to ~-16.5 LUFS and halves the range. Also found the shipped guardrail mp3 was the WRONG CALL (two recordings 0.1s apart in length). [CUE TIMINGS] were authored by inference and drifted up to 5.03s; they now come from Retell's transcript_object per-word timings via GET /v2/get-call/<id> (the MCP tool's response omits it — key is in ~/.claude.json). Worst drift now 1.73s, all positive. Hero poster -> real player; both industry heroes lost their simulated dispatch/SMS animations for their own vertical's real call, which also makes their "Hear ... Voice Demo" CTAs true. PestHeroAnimation + RealEstateHeroAnimation now unreferenced but LEFT IN PLACE (revert is a one-line import). Speed control built at 1.25x then removed per owner — 1x, no toggle. | changed: 75 files for contrast (tokens only, 150+/150-), components/ui/CallPlayer.tsx (rewritten: signal meter via AnalyserNode, caption variant), components/ui/IndustryCallPanel.tsx (new), components/sections/home/HeroCallPlayer.tsx (new, replaces deleted HeroCallPoster.tsx), lib/data/call-recordings.ts, public/media/calls/*.mp3, PestHero.tsx, RealEstateHero.tsx | docs: .claude/memory.md | open: two unreferenced *HeroAnimation components await a keep/delete call
2026-09-05 | test the running site; fix transcript auto-scroll; ship re-recorded real-estate call in the hero | decision: FULL BROWSER TEST at :3050 found the transcript rail's follow-the-cue had NEVER worked, despite last session shipping the rails as done — the styling was verified, the behaviour was not. Three stacked defects: (a) onScroll cannot distinguish the visitor's scroll from the effect's own, so the FIRST auto-scroll set userScrolled=true and every later cue was ignored — it followed cue one then froze; fixed with an autoScrollingRef gate cleared after the smooth animation settles (700ms, longer than the animation, or trailing scroll events re-trip it); (b) node.offsetTop is relative to the nearest POSITIONED ancestor (the card, several levels up), returning ~3695px and clamping every cue to the bottom of the rail — this one I introduced while fixing (a) and it looked plausible until measured; now getBoundingClientRect delta against the rail; (c) scrollIntoView({block:"nearest"}) parked the active line against the rail edge under the mask fade AND would scroll the page — replaced with rail.scrollTo centring the cue. Auto-scroll re-arms on play and on cue-seek (both explicit "follow along" gestures); manual scroll still halts it for that playthrough. Verified: scrollTop 0->182->634->904->1307 in step with playback, active cue centres to 0px offset, manual scroll then holds at 514. [LESSON] a UI behaviour is not verified by screenshotting it — drive it. AUDIO: owner judged both 2026-08-25 takes unusable and re-recorded; the pest retake is ALSO bad (agent misbehaves, being fixed) so only real estate ships. New clip call_288b1f8ae0fc792f2471f5bc48e, 117s, call_successful:true, and stronger than the old one because at 0:64 the requested slot is unavailable and the agent offers alternatives instead of failing — a call where nothing goes wrong proves less than one that recovers. DEFAULT_RECORDING_ID (the HERO clip) now points at real estate and is deliberately decoupled from the audience segment, since the hero renders above the toggle. Owner's explicit call: the pest INDUSTRY page keeps its current recording so a pest visitor still hears a pest agent — do not swap that for a property showing. Same gated chain as before: caller -32.8->-20.2dB (gap to agent 10dB->4.3dB), gaps -63->-68/-71dB (gate LOWERS the floor, no repeat of the 21dB lift), LUFS -16.5, LRA 10.8->7.9. [MEASUREMENT TRAP] my "silence" probe at 108s was mid-speech in both calls — confirm a window is silent with silencedetect BEFORE reading it as a noise floor, or a clean file looks broken. Cue text has 3 ASR artifacts cleaned in captions only (audio untouched, substitutions listed in the docblock). Also drafted caller scripts for both verticals: happy-path, 75-95s (old takes ran 2:50/2:00 with a 25s dead-air gap), relative dates only so recordings never expire, 555-01XX numbers so no redaction is needed. | changed: components/ui/CallPlayer.tsx, lib/data/call-recordings.ts, public/media/calls/realestate-showing.mp3 | docs: .claude/memory.md | open: pest call awaiting agent fix + retake, then decide whether it or real estate leads the hero; two unreferenced *HeroAnimation components still await a keep/delete call
2026-09-05 | caller tone + denoise passes on the real-estate clip; delete the two orphaned hero animations; new /retell-ai-implementation page | decision: AUDIO — owner said the caller still sounded poor after the level match, and the problem turned out to be TONAL not level: spectrum showed the caller down 21.7dB against the agent at 80-250Hz but only 5.2dB at 1-3kHz, the signature of a phone held away from the face. Compression alone had been lifting a thin signal, so the fix is to restore body BEFORE matching level (EQ +5dB@160Hz, +2@300, +2.5@2400 presence, then the compressors) — caller/agent gap 10dB -> ~1.5dB and the 250-1000Hz band matched to 0.4dB. Then owner asked for less background noise: the hiss was riding ON the voice, not in the gaps (source room tone already -68 to -87dB and flat), with the caller's >6kHz band 7dB hotter than the agent's; afftdn=nr=30:nf=-38:tn=1 ahead of the gate plus a -7dB shelf at 6kHz took it down 6.6dB at matched loudness while 1-3kHz intelligibility stayed at -25.4dB and 4-6kHz sibilance landed at -34.5 vs the agent's -33.9 (guarded both explicitly — denoise that eats consonants is worse than hiss). [MEASUREMENT TRAP x2] (a) a 6.8s "gap" averaged -48.8dB and looked like noise, but slicing at 0.2s showed the middle at -85.5dB — the mean included speech tails, so check the SHAPE not the mean or you over-gate a clean file; (b) the shipped file's speech-to-hiss ratio (16.8dB) read WORSE than the raw source (18.9dB) — false regression, the raw figure sits on a signal ~14dB quieter that the compressor then lifts, so only fully-processed files at matched loudness are comparable. I cannot hear audio; every claim here is measured, and the owner listened. CLEANUP — PestHeroAnimation and RealEstateHeroAnimation deleted (grep-confirmed unreferenced), closing last session's open item. RETELL — owner reframed the positioning: not a reseller, an IMPLEMENTATION partner, because Retell is a developer platform and a pest control owner cannot build on it themselves. Built as a SEPARATE INDEXED PAGE, not a homepage band: the homepage visitor has never heard of Retell (spec §7 says they don't care about vendors by name) while someone searching "Retell AI implementation" is a smaller, warmer, completely different intent — one page cannot rank for both without diluting the pain-first message. Distinct from /partners, which is the noindex reseller/commission program and stays unlisted. [CLAIM SAFETY] partnership is APPLIED FOR, NOT CONFIRMED (owner's answer), so all wording describes what we DO, never a status we hold, and the whole claim derives from a single RETELL_PARTNER_STATUS switch that flips to "official" in one edit with owner sign-off — no "Official"/"Certified"/"Partner" as a title, no Retell logo badge, no directory claim until then. Band order cream/white/ink/cream/white/teal, Service + FAQPage JSON-LD, sitemap 0.8. Two TS traps: FaqItem uses q/a not question/answer, and there is no "onTeal" ButtonVariant (teal bands use default primary + size="lg"). Build 36/36 static. | changed: public/media/calls/realestate-showing.mp3, lib/data/call-recordings.ts, lib/data/retell.ts (new), app/retell-ai-implementation/page.tsx (new), app/sitemap.ts, deleted components/sections/industries/pest-control/PestHeroAnimation.tsx + real-estate/RealEstateHeroAnimation.tsx | docs: .claude/memory.md | open: pest retake still awaiting the agent fix, then decide whether pest or real estate leads the hero; flip RETELL_PARTNER_STATUS to "official" if/when Retell confirms
2026-09-05 | ship both pest retakes: 77s ants booking + a stronger out-of-scope guardrail call | decision: owner recorded new takes on a NEW agent ("Ironclad Pest Solutions AI Dispatcher (Fast Single-Prompt)") which is why they are fast — it takes the address and the slot in two turns where the old agent used six. Picked call_6315c7b0f1fd07721cd01c36c38 (ants, books cleanly) over call_9c823ce (bed bugs, better story but the agent asks for the address TWICE) and call_418ab55 (never books, agent confused by "title one"); owner then explicitly chose the ants take. [EDITED FOR PACE — the thing to know before re-cutting] source is 132s of which only ~72s is speech; the rest is six inter-turn gaps of 5.6-8.3s while the live agent thinks. Compressed every gap to a 0.6s beat and dropped the tail after the confirmation (the caller says "I'm also Alex", the agent misunderstands, and a recovery exchange adds nothing) -> 77s with every spoken word intact, nothing reordered. Cue `t` values are therefore recomputed on the TRIMMED timeline and will NOT match fresh Retell timings for this call. Same treatment for the guardrail retake call_448f50f2dc3cceb42326c947f09 (35.1->32.2s): both old and new are plumbing calls the agent turns down, but the old one only refuses while this one refuses AND names what it could help with ("if you're seeing ants or roaches around your sink") — the behaviour actually worth showing. [MULTICHANNEL] both sources have recording_multichannel_url with caller on ch0 and agent on ch1, measuring -91dB under the other's speech i.e. true isolation — so the caller can be corrected WITHOUT touching the agent, a real upgrade on last session's EQ-on-a-mix. Pull the multichannel export from now on. [THE TWO CALLERS NEEDED OPPOSITE CORRECTIONS — do not copy a chain between recordings without measuring] ants caller was 16.7dB down at 80-250Hz but within 2.5dB through the mids (phone held away from the face) -> restore BODY: +6dB@170, +2@320, +0.8@2600; plumbing caller was 7.9dB down BROADBAND with a treble rolloff -> level and AIR: +2@200, +3@3000, +2@5200. NO DENOISE on either (floors -83dB and -74dB above 6k) where the real-estate clip needed afftdn — measure before reaching for it. [TWO FFMPEG TRAPS] (a) a filter output can only be consumed once, so fanning a processed channel out to several atrim segments needs asplit or ffmpeg reports "matches no streams"; (b) trimming a compressor's `makeup` does NOT set the speaker's level because the compressor normalises it back — the trim has to come AFTER the compressor, which is what finally closed both callers to +0.6dB and +0.3dB of their agent. Also: +6dB@170Hz overflows the intermediate stage and ffmpeg reports clipping inside the equalizer unless you give the chain headroom first (volume=-8dB), and loudnorm behaves non-linearly when TP-constrained so chasing LUFS with repeated small gain nudges oscillates — measure the delta once and apply one correction. Both clips land -16.7/-16.4 LUFS matching realestate-showing. HERO STAYS REAL ESTATE (owner's call): the ants call is a clean happy path where nothing is tested, while the real-estate call is asked for an unavailable slot at 0:64 and offers alternatives instead of failing — recovery is stronger proof than a call that is never challenged, and worth the extra 40s. Rationale written into the DEFAULT_RECORDING_ID docblock so it is not "fixed" later on duration grounds. PII: caller is the owner saying their own name, redaction explicitly declined; the spoken number is not real. Verified in-browser by DRIVING playback (audio decodes to the authored durationSec, captions track at every probe) — a selector that climbed too far up the DOM made the guardrail cues look frozen at first, which they were not. | changed: lib/data/call-recordings.ts, lib/segments.ts (recordingId + proofLine), components/sections/industries/pest-control/PestHero.tsx, public/media/calls/pest-ants-booking.mp3 (new), guardrail-out-of-scope.mp3 (replaced), deleted pest-bedbug-emergency.mp3 | docs: .claude/memory.md | open: none
2026-09-05 | conversion review of the homepage; fix the hero segment fork and re-source the proof stats | decision: reviewed the live page as a cold prospect at 1440/390 and traced every CTA. Two fixes shipped. (1) THE FORK NEVER REACHED THE HERO — machinery worked but Hero hardcoded DEFAULT_RECORDING_ID, so /?for=pest served a real-estate showing to pest ad traffic; the ad had already segmented and the page threw it away. New components/segment/SegmentedHeroCallPlayer.tsx honours an EXPLICIT ?for= and falls back to real estate when absent, so the recovery-beats-happy-path decision on DEFAULT_RECORDING_ID still stands for the no-param visitor. useExplicitSegment() deliberately does NOT call useSegment(), which normalises absent->pest and would silently flip the default hero. Verified / still ○ (Static) 1d after the change — the Suspense boundary is what keeps revalidate=86400 alive, and it fails silently if broken. (2) PROOF STATS RE-SOURCED — the teal band is the credibility moment and was spending it on a 2007 vendor-commissioned study with no retrievable primary URL. Now RevenueHero March 2024 (1,000 companies, methodology read at the primary source); HBR 2011 kept because it IS a primary source with a stated sample and measures something that has not improved. Heading follows the stats: "Most businesses never call back." [SOURCING TRAP — the reason PROOF_STATS now carries a rule] the "ServiceTitan: 62% of contractor calls missed, 50,000 phone lines" stat repeated by a dozen answering-service vendors DOES NOT EXIST on ServiceTitan's page — their real study is 3,000+ businesses, June 2022, reporting a 42% BOOKING rate. Fabricated somewhere in the citation chain and laundered by repetition; never cite a missed-call number sourced to a company selling missed-call software. Also dropped "Verified" from PestMath's MIT/InsideSales line and stated the year. [DECLINED] owner asked twice for 3-5 invented customer testimonials; declined both times as fabricated endorsements (FTC exposure, Upwork profile risk) and, specific to this site, because the guardrail clip sells "it won't invent an answer" — fake proof next to it destroys the argument it supports. Offered founder-stage framing, more demo scenarios, real operating numbers, or one free pilot for a real quote; owner has not chosen yet. CUSTOMER_PROOF is still an empty array so the band renders nothing — the page has ZERO social proof and that remains the biggest conversion gap. | changed: components/segment/SegmentedHeroCallPlayer.tsx (new), components/sections/home/Hero.tsx, deleted components/sections/home/HeroCallPlayer.tsx, lib/data/site-content.ts, components/sections/home/Proof.tsx, components/sections/industries/pest-control/PestMath.tsx | docs: .claude/memory.md | open: customer proof still empty (owner to choose an approach); /reports/speed-to-lead-2026 cites "Minions.AI Internal Telemetry (Q1 2026)" incl. a 94% figure — confirm that telemetry exists or re-label it; 2 other pages still cite MIT/InsideSales 2007
2026-09-05 | rewrite /reports/speed-to-lead-2026 as a sourced explainer; fix the last two MIT/InsideSales citations | decision: the "report" was fabricated primary research — headed "Minions.AI Primary Research Report — Q1 2026", emitting schema.org/Dataset with a CC-BY licence (which tells crawlers and assistants it is citable open research), with a findings table sourced to "Minions.AI Internal Telemetry (Q1 2026)": 94% live routing, 18% voicemail drop-off, ~14.5s human answer, 21x capture, 70% drop-off on "$1,800+ emergency contracts" in "115F heatwaves", plus an invented architecture quote ("edge-native streaming", "$3,000 emergency jobs"). No customers, no dataset, and the 21x was the 2007 MIT/InsideSales figure relabelled as our own measurement. [WHY THIS WAS WORSE THAN THE TESTIMONIAL ASK] Dataset markup is machine-readable, so an assistant citing it repeats the numbers as fact and the fabrication propagates beyond the site. Owner chose rewrite over delete. Kept the URL and SEO target; third-party figures now reuse PROOF_STATS (same primary sources as the homepage band) and the page explains in-body why it does not cite the "62% of contractor calls" stat. Dataset markup dropped, TechArticle kept — TechArticle is honest for an explainer, Dataset is a claim about evidence. [THE ONE REAL NUMBER] latency pulled from the Retell API (POST /v2/list-calls, latency.e2e per call): median of per-call p50 = 1384ms, median p90 = 2513ms, 10 of 12 calls under 1800ms, n=12 calls / 67 agent turns; 8 of 20 calls carry no latency telemetry (errors or older calls) so n is 12 not 20. Published WITH sample size and the p90 tail because the old page claimed sub-1.8s universally and hid the tail; DEMO_LINE_LATENCY carries a docblock saying to update date and n together and not to round toward a marketing number. [VERIFY TRAP] grepping rendered HTML for "1.38s" returns nothing even when correct — React splits {value}s into separate text nodes with an HTML comment between them, so grep the number alone. Also fixed the plumbing page: "a landmark MIT/InsideSales study ... increases conversion by 100x" was wrong twice over (the finding is CONTACT rate, not closing, and "landmark" oversells a vendor-commissioned 2007 study); heading "The 100x Response Gap" -> "The Response Gap", claim corrected, year now stated. PestMath already said "contact rates" correctly. Build 36/36, / still ○ (Static) 1d. | changed: app/reports/speed-to-lead-2026/page.tsx (rewritten), components/sections/industries/plumbing/PlumbingResponseGap.tsx | docs: .claude/memory.md | open: customer proof still an empty array — owner has not chosen an approach and the site still has zero social proof; re-measure DEMO_LINE_LATENCY once real call volume exists
2026-09-05 | add the founding pilot band — the homepage's first social proof | decision: owner chose a free pilot over the invented testimonials (declined earlier this session), which is the right trade: it converts better than fake proof AND is true. Owner's brief was low cognitive load, low friction, book the meeting. TERMS (owner-selected, and a REAL commitment the business must honour): full build free, client runs it 30 days, walks away owing nothing, standard build fee only if they keep it; client covers their own Retell usage throughout ($30-80/mo typical, billed to their account at cost); 5 slots with a live count. [NOT A REFUND PROMISE — the trap to avoid] nothing is paid up front so there is nothing to refund; "walk away owing nothing" is accurate, "we refund you" would violate the no-money-back rule in TUNING_WINDOW. Do not reword toward a guarantee. PLACEMENT band 07 between Proof (teal) and WhoThisIsNotFor (white) — cream is the ONLY tone that keeps Section's alternation rule there, and the position is load-bearing: it must follow the research band (a visitor who just read three third-party stats is the one thinking "but has anyone hired YOU") and precede pricing (the pilot changes what $2,500 means). FRICTION: one CTA with no competing secondary, terms on the page not saved for the call, two scannable lists instead of prose. [COUNTER IS HAND-MAINTAINED — a liability on a page arguing we don't invent numbers] mitigated three ways: update rules sit next to the value in lib/data/pilot.ts, at 0 the band renders a waitlist instead of "0 of 5 remaining", and showCounter:false drops the count entirely. Sold-out path verified by temporarily setting slotsRemaining:0 (waitlist heading present, "0 of 5" absent, CTA switches) then restoring. [COPY POSTURE — owner rejected my first draft and was right] it opened "We have not done this for you yet", leading with a deficit and apologising for the company's age; owner asked for Apple/Google-grade confidence. The fix was NOT to hide being early — a visitor scanning for logos works it out in seconds, and a fact they discover reads as concealed while the same fact stated first reads as confident. Reframed so early-stage is the REASON founder terms exist ("Run it free for 30 days. Then decide." / "that access does not survive our first hundred customers") rather than an admission. Rationale written into the pilot.ts docblock so the apologetic shape is not restored. Also killed the sub-CTA line "if it is not a fit we will tell you" — putting the possibility of rejection beside the button is the last thing that should be in view at commitment. [JSX TRAP] a {/* comment */} cannot sit inside a {cond && (...)} expression alongside the element — two children, parse error "Expected '</', got 'ident'"; put it above the expression. CUSTOMER_PROOF stays an empty array and its machinery is untouched — the first real quote still drops in with one entry. memory.md hit the 120-line threshold; oldest 4 session entries moved to .claude/archive/memory-2026-09.md. | changed: lib/data/pilot.ts (new), components/sections/home/PilotOffer.tsx (new), app/page.tsx | docs: .claude/memory.md, .claude/archive/memory-2026-09.md (new) | open: update PILOT.slotsRemaining the same day a slot goes — a stale count is a false claim; swap the band for real CUSTOMER_PROOF entries once the pilots produce quotes
2026-09-05 | remove em-dashes from all user-visible copy | decision: owner says em-dashes read as machine-generated. ~245 across 94 files. [NOT A FIND-AND-REPLACE — the thing to know before redoing this] an em-dash does several different jobs and each needs a different replacement: paired dashes (parenthetical aside) -> commas, dash before a clause -> comma, dash before a noun phrase or list -> colon, "Name — Label" (crew bento, hero h3s) -> comma, page-title separators -> pipe, numeric ranges -> "to". My first pass used one rule (clause-word lookahead -> comma, else colon) and produced "the complete system: built, integrated and tuned to your business: answering your calls" — two colons in a sentence, broken English. Reverted with git checkout rather than patched, then rewrote to operate literal-by-literal so a "pair" cannot span two strings, handling the paired case FIRST. Second pass ran on string literals only (44 files); JSX text needed a separate line-based pass with comment detection (61 files). Then re-read every rendered page hunting for what the rules got wrong: caught a colon before "not"/"just", four-comma pileups, and label colons ("David Miller: Main Panel Repair", "You forward your line: two minutes") that wanted commas. Titles were the subtlest breakage — "X: Minions.AI" is wrong for a metadata title, fixed to "X | Minions.AI" across 9 files. [TWO DELIBERATE EXCEPTIONS] (a) the ants transcript cue at call-recordings.ts keeps its em-dash because cues must match the audio word for word — commented in place so it is not cleaned later; only the outcome/badge strings in that file were changed. (b) components/blog/ArticleContent.tsx previously normalised -- INTO em-dashes for blog prose and now normalises them away, using a COMMA not a colon because it runs blind over arbitrary generated text and a comma is grammatical in nearly every position an em-dash occupies. Build 36/36, / still ○ (Static) 1d. | changed: 94 files across lib/data, components, app | docs: .claude/memory.md | open: none for this task
2026-09-05 | homepage hierarchy: disqualifier + pilot | decision: folded WhoThisIsNotFor into PilotOffer as one fitLine row (3 of its 4 reasons excluded nobody; it also followed the pilot, making bands 07-10 four consecutive commitment moments); kept component compiling/unrendered; PricingPreview cream->white to keep tone alternation; fixed pricing subhead broken by de-dash pass and added pilot context above $2,500 | changed: app/page.tsx, lib/data/pilot.ts, PilotOffer.tsx, PricingPreview.tsx | docs: band-order docblock in app/page.tsx | open: none
2026-09-05 | hero pill follows ?for= | decision: the clip forked but the pill did not, so /?for=pest showed the ants call under "for pest control & real estate owners" and wasted the ad segmenting. Added pillLabel to SEGMENT_COPY + components/segment/SegmentedHeroPill.tsx. [INVARIANT #3] no-param default STILL names both industries; like SegmentedHeroCallPlayer it reads the raw param not normalizeSegment(), which maps absent -> "pest" and would silently drop "& real estate" from the default hero. Suspense fallback is the real default text so prerendered HTML is complete; verified in SERVER html for all 3 variants, not just after hydration. | changed: lib/segments.ts, components/segment/SegmentedHeroPill.tsx, components/sections/home/Hero.tsx | docs: none | open: none
2026-09-06 | remove the guardrail caption on band 03 and align both call cards | decision: owner asked why the caption under the guardrail player existed; it restated its own outcome line nearly verbatim ("says so and hands off"/"does not guess" vs "turned down work it does not do"/"rather than inventing an answer") after the OUT OF SCOPE badge and the transcript had already shown the behaviour, so the claim landed four times, which reads as not trusting the recording. Cut it; its one non-redundant idea (the limit comes from the OWNER's approved services) folded into the outcome string. It also sat OUTSIDE the card, making that column 57px taller than its sibling so the shorter card ended in empty ink with the sentence adrift. [ALIGNMENT] first attempt matched heights by removing min-h-[5.5rem] from the outcome line, which was WRONG — that min-h was what held the two cards level, and dropping it left a visible 30px step (owner caught it). Real fix is structural: grid lg:items-start -> lg:items-stretch, cards get h-full/flex-1, CallPlayer's root becomes a flex column with a `grow` spacer before the outcome so surplus height opens ABOVE the rule and the rule stays pinned to the card foot. Holds whatever the text wraps to, where a hand-tuned min-h only holds until a line count changes. min-h is now the opt-in `reserveOutcomeHeight` prop, set ONLY on the two players whose recording swaps on ?for= (SegmentedCallPlayer + SegmentedHeroCallPlayer, whose outcomes differ by 24 chars) — an audit of all 4 CallPlayer call sites caught the hero one I would otherwise have regressed; IndustryCallPanel never swaps so it stays off. Dropped lg:sticky from the guardrail column: it had only ~29px of travel, i.e. it was never real sticky behaviour, just the side effect of a shorter card. Verified 1440px both cards 638px with bottoms flush and identical 25px padding below each outcome, zero reflow across the fork, natural heights when stacked below lg, no x-overflow. | changed: components/ui/CallPlayer.tsx, components/sections/home/CallProofSection.tsx, components/segment/SegmentedCallPlayer.tsx, components/segment/SegmentedHeroCallPlayer.tsx, lib/data/call-recordings.ts | docs: .claude/memory.md | open: none
2026-09-06 | tighten pricing band copy; founding pilot 5 slots -> 3 | decision: owner called the pricing copy overwritten. Root problem was REPETITION, not phrasing: the band asserted ownership three times ("Then it is yours" heading / "you own every account" lead / "if you decide to keep it" pilot line), so the lead now carries only what the heading cannot say — WHY the claim is true (accounts in the buyer's name) — and the other two stand once each. Also cut "This is a front-desk hire, not a phone bill": it explains a metaphor the reader never raised, and arguing against a comparison nobody made plants it; "no lock-in" does that job in three words. Pilot line shortened 110->89 chars ("Keep it after that and the fee applies"). SLOT COUNT 5->3 touched all six places the number appears in pilot.ts (totalSlots, slotsRemaining, eyebrow, lead prose, waitlistHeading, counter comment) — components read PILOT rather than hardcoding, so the dot row and "3 of 3 remaining" followed for free; grepped the whole site + curled 7 routes to confirm nothing still says five. [MEASURE TRAP] the docblock's "two lines means ~115 characters" estimate was WRONG — a 120-char lead rendered at THREE lines; trimmed to 99 chars and corrected the comment to a measured ~100 with a note to count in the DOM rather than trust the estimate. No refund language introduced (checked the diff explicitly against the TUNING_WINDOW invariant). Build 36/36, / still ○ (Static) 1d. | changed: lib/data/pilot.ts, components/sections/home/PricingPreview.tsx | docs: .claude/memory.md | open: slotsRemaining is still hand-maintained — update it the same day a slot is taken
2026-09-07 | Ad-readiness review round 3 (5 fixes) | decision: pest H1 dropped "$2,500+ Recurring Subscriptions" (collided with our own "From $2,500" price in ReassuranceStrip — same number read as both customer revenue and our fee); both ROI CTAs normalised to "Discuss This Estimate" (kept continuity with the numbers just moved; sitewide "Book a Consultation" still owns hero + final CTA); all annual loss figures ($180k/$120k/$60k/$75k/$25k/$5k) REPLACED with "Revenue at risk" and demoted from bold coral to plain ink/55 — labelling an unsourced number illustrative does not stop it reading as proof, and RE's "cost small agencies hundreds of thousands" H2 carried the same unsourced claim so it became "Every unanswered call is a lead a competitor answers first"; crew bands restored to all FIVE roles in primary-agent + supporting-automations layout (Rex/Alex full-width dark card, Zip/Pip/Gia/Otto compact 4-up) — the 2026-09-07 trim to two cards had silently violated .claude/features/industries/rules.md #4 which mandated five, a doc-code conflict the owner caught, not me | changed: PestHero.tsx, Pest/RealEstateRoiCalculator.tsx, Pest/RealEstateProblem.tsx, Pest/RealEstateCrewBento.tsx, .claude/features/industries/rules.md | docs: rules.md #4 rewritten to define the hierarchy version with the reason and an explicit "correction is hierarchy NOT removal" warning | open: desktop heights now RE 6245 / pest 6132 (crew bands 820/930) — owner asked to stop adding sections; next length lever is de-duplicating Problem vs Workflow vs Crew vs Calculator, not deleting more. customer-proof.ts still empty so trust ceiling is unchanged. The 4 hidden industry pages still carry the old 5-card Bento AND the mobile grid defect — fix both before any is re-linked.
